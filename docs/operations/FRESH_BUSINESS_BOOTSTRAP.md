# Fresh-business first Admin and outlet bootstrap (MT-7.5)

> **Current bootstrap safety procedure; historical test-status narrative below is from the 20-Sep checkpoint.** Later protected first-outlet/Admin and cross-family Edge test outcomes are recorded in the chronological project ledger; do not treat the older `UNVERIFIED` paragraphs as the active gate list. Production provisioning remains separately unauthorized absent explicit owner instruction.

Authority: `docs/audit/MT-7.5_FRESH_BUSINESS_SCOPE.md`; the owner has excluded ALL old POS/Website business-row, identity, snapshot and cutover migration. The old repositories/databases are immutable read-only functional references, not deployment inputs. No actual production provisioning is approved or executed by this document.

On an explicitly approved NEW target, first apply the canonical target schema and verify that the required system `business_profiles` singleton, protected `Full Access` role and permission definitions exist. Do NOT seed development/demo accounts, outlets, products, customers or transactions in production. Use only a securely configured target MySQL, HTTPS/Admin origins, non-committed APP_KEY and private runtime credentials. These are future operating prerequisites, not claims that any real production database has been deployed.

From `backend/`, the authorized operator runs `php artisan setup:first-admin` in a private INTERACTIVE terminal, then supplies their new Admin name, email, strong unique password and password confirmation at the prompts. The password is hidden and is NEVER accepted from CLI arguments, committed seeds, `.env` defaults or application logs. Never run it with `--no-interaction`; never use an old POS/Website account or import its hash. A mismatch or invalid input creates no account. The service locks the canonical business-profile row and allows exactly one initial account only when Admins and its explicitly guarded business tables contain zero rows; it requires the protected system Full Access role and logs a non-secret initial-provisioning identity audit. A second setup or a preexisting outlet/customer/transaction fails closed. Do not use this one-time command for later account repair or recovery.

The same guarded transaction installs only the canonical protected product categories, device/unit options and buying-source defaults required by the new POS. It creates no outlet, product, stock, customer or transaction. A partial/imported master-data set makes first-Admin setup fail closed instead of mixing authorities.

The new owner can now sign in at the approved Admin/POS login WITHOUT an assigned outlet, enter protected `Manage outlets`, create and configure the FIRST outlet by confirming their current password, and explicitly select the newly assigned outlet before accessing inventory/sales. Ordinary unassigned Team Members still cannot sign in to operational POS; no unselected outlet receives shop access. Then create new products, inventory and customers through the actual app; no legacy row is needed. Preserve future generated record history and archival safeguards.

Verification boundaries: local isolated `FirstAdminProvisioningTest` 3/3 (27 assertions) and the existing first-outlet login HTTP test together 4/4 (70 assertions) PASS on 20-Sep-2026; neither creates a real owner. A separate isolated zero-business-schema acceptance now PASSED (30 migrations, 171 tables, 17 business tables empty, synthetic first owner only; see current ledger). Actual production provisioning, first-outlet real Edge acceptance and joined inventory/sales/warranty/reporting Website journeys remain UNVERIFIED. One synthetic first-outlet Edge attempt reached rendered inventory but exceeded the 30-second whole-test window; its corrected attempt could not reach setup because two subsequent browser-runner startups stalled. Those non-terminal runs are NOT PASS, and unverified test-fixture edits were not committed. Diagnose runner startup with materially different verified strategy before a further browser run; do not repeat identical runner attempts. Mock Gmail/payment tests cannot establish authentic provider connectivity, which requires later credentials and express authorization. GitHub CI belongs only at authorized milestone/final gate.

Latest focused Edge diagnostic (20-Sep-2026): the new opt-in browser test was withdrawn because a trace-instrumented single runner attempt remained idle before global setup/webServer and emitted no logs for ~95 seconds, even though CLI `--list` completed promptly. Do not repeat the identical startup; separate Playwright framework/bootstrap processes from the application under a materially different controlled diagnostic. Preserve already accepted backend provisioning/login tests and the clean target-schema evidence; no source-data import or live external credentials are required.
