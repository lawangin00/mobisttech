# MT-7.5 / W05 real Admin -> Customer status journey (24-Sep-2026 LOCAL)

Existing `18/W05` development-family gate only; W05 OPEN [E], MT-7.5 16/27 DONE / 11 OPEN, genuine W04/H-02 separately PRE-LAUNCH PENDING. Exact parent `37d8a8e873fe6b7428b86de08b12804bd117d995`. No application code/merchant activation or unapproved original-source activity. This is one bounded, previously missing actual-API/UI lifecycle acceptance, not the entire W05 family closure.

- The already verified real Website public enquiry -> Laravel lead -> authenticated Admin project/proposal approval -> signed-in Customer detail test now also drives the **real, unmocked protected Admin React UI** through `approved -> in_progress -> review -> delivered`, observing HTTP PATCH 200 and returned exact state for EACH transition. Customer's fresh real Website portal reads `delivered`, the preserved exact approved PKR 10000.00 scope/milestone, and exactly three new immutable `project status changed` history rows, then retains authorized historical detail in `commerce_only`. The companion unchanged real Admin delivery -> Customer private download + unpaid completion HTTP 409 remains independently accepted; no real wallet/card settlement, completed/closed paid milestone or all-state round-trip is claimed by this check.
- First expanded focused attempt TIMEOUT 150s after proposal approval before any transition PATCH; Playwright trace showed the test clicked `Transition project` while Admin approval's follow-up GET/React busy state had not finished, so the button was not yet enabled (FIXTURE / asynchronous UI orchestration, NOT an internal payment/product defect). A concrete correction awaits disappearance of the approved proposal's `Approve proposal` button after the real project-detail refresh and explicitly requires `Transition project` enabled after selecting each next status. Focused real Edge **1/1 PASS (48.2s)** after correction and immediately affected two-W05 real Admin/Customer neighboring join **2/2 PASS (1.1m)** with terminal exit 0 and guarded owned synthetic fixture teardown, same exclusive LOCAL test DB lock. No unchanged failed attempt was treated as PASS.
- Previously accepted unchanged PHP `ClientProjectServicesTest` service/status/expiry/owner and W05 foreign-customer negative evidence remains reusable; the latest W04 PHP joined run `117/117 PASS (1913 assertions)` includes the six W05 ClientProjectServicesTest cases, unchanged application code and same disposable DB. No full project/backend or hosted regression repeated for this browser-only scoped acceptance.

Next genuine W05 acceptance is to reconcile **unverified** real Customer/Admin mode and expiry/foreign-owner negatives and paid completed/closed terminal state with existing service-level synthetic evidence, or document a specific missing case and finish a bounded family join before marking `18/W05` DONE. Approved original W05 24-file/9-route disposition and W04 development closure are complete; do not reopen without proven regression. H-02 PRE-LAUNCH official merchant inputs, genuine refunds/settlement/activation stay BLOCKED with external methods OFF.
