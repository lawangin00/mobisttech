# MT-7.5 W05 joined real Admin/Customer browser checkpoint - 24-Sep-2026 PKT

Status: W05 OPEN [E], MT-7.5 15/27 DONE / 12 OPEN. Exact parent before this patch `94f9da2`; all changes confined to project-owned W05 browser acceptance, evidence and ledger. H-02 actual providers remain HOLD.

- Reused the existing browser-website fixture for fresh synthetic Customer, project, published Website mode and digital Admin account. No old source data, production row, real vendor or hosted CI was used. Real browser traffic (NOT intercepted Admin endpoints) establishes one protected Admin to Customer private delivery/download: linked project, authorized delivery POST HTTP 200, Customer-owned project read and downloadable file name. The real protected completion mutation correctly returns HTTP 409 with an unpaid approved milestone.
- New public Website enquiry -> actual Laravel lead -> protected Admin project creation -> versioned 10000.00-PKR proposal approval -> real Customer account project/milestone projection -> commerce-only historical visibility also passed as one joined test. The chosen new project has no actual externally paid receipt; no real payment or completed project was claimed.
- The first independent new W05 delivery test originally asserted HTTP 201 but the established Admin upload contract returns 200. After correcting test expectation it passed focused 1/1. A later added completion-denial assertion expected a detailed source exception in the UI but the identity boundary intentionally emits generic `Request failed.`; real HTTP 409 and generic UI feedback then passed focused 1/1.
- On the first six-case browser run, five tests passed and the new sixth hit a 150-second timeout after successful public enquiry because protected Admin login returned HTTP 403. Trace showed the test waiting for a successful redirect. The initial attempt to await logout via the existing POS helper was NOT a material fix: that helper uses a relative route resolved against Website port 13000, not Laravel Admin port 18080; a two-W05 run again reached the same HTTP 403. LOOP_GUARD stopped equivalent repeats. Root cause isolated to the test's Admin session lifecycle: previous case's logout was not completed on the actual Laravel origin, leaving an active desktop slot protected by `PosSessions::register`. The material correction introduces test-only `releaseWebsiteTestAdmin` with explicit localhost:18080 Admin navigation, HTTP 200 logout response and successful login-page redirect; no account-session/product/device-policy relaxation, bulk session reset or unowned data purge.
- After this corrected session lifecycle, exclusive cooperative `tools/run_isolated_test.py` guarded sequential REAL Edge W05 filtered 2/2 PASS (59.0s) followed by the joined `client-project.spec.ts` family and impacted W04 neighbors 6/6 PASS (1.5m). Both setup and teardown completed, no overlapping runner was detected at preflight; no final entire-schema residue PASS is inferred from fixture-teardown messages alone. Existing backend money/ownership/expiry unit evidence is reused only where its code/config/contract/fixture dependencies are unaffected by this browser-only patch.
- Remaining for W05 family closure: independently verify full approved Customer/Admin status transitions and explicit foreign-owner/privacy/expiry/mode negatives on the current candidate, including any necessary joined HTTP proof. Do not mass-mark W05 DONE based on two narrow E2E cases, repeat completed 24/9 source mapping, or require genuine wallet/card settlement as a synthetic W05 PASS. Q01 exact final milestone remains separate.
