# MT-7.5 W04 — checkout browser readiness race (23-Sep-2026)

Status: bounded browser-test synchronization; NOT provider approval or W04 closure.

- Before the correction, two separate full five-case checkout runs reproduced the same third-case `JazzCash` locator failure. The failure screenshot recorded `Loading checkout…`, not a disabled channel or rejection. The same unsafe-redirect test passed on an isolated rerun.
- Independent Playwright trace inspection of the failed third case (`checkout.spec.ts:130`, retained local trace ZIP) recorded `GET /api/customer/checkout/channels` HTTP 200 in 32.732 ms while the concurrent authenticated `GET /api/customer/account` HTTP 200 took **4898.818 ms**. Checkout hydrates its radio controls only after `Promise.all([account, channels])` resolves. The 5-second locator expectation therefore raced the slow account request; this was a test synchronization defect, not evidence that the payment channel became unavailable.
- Added shared `checkoutReady(page)` in `backend/tests/browser-website/checkout.spec.ts`: subscribe to both successful HTTP responses before navigation, await both, and then assert all four hydrated channel inputs within a bounded 15-second UI timeout. Four existing checkout cases use this helper. No payment services, provider adapters, application timeouts, merchant settings or production UI were changed.
- Local TypeScript check passed; scoped isolated-browser checkout suite passed **5/5**, including COD, synthetic hosted-initiation failure recovery, blocked unsafe redirect, all-unavailable channels, and digital-only mode. Global disposable-fixture teardown completed. Existing Next.js `destination stream closed early` server warnings were observed during passing runs; their exact separate origin is not established by this fix.
- Follow-up: repeat the complete five-case suite and verify clean data/service state before accepting this browser checkpoint. External merchant integration, authentic callbacks/refunds and settlement remain H-02 HOLD.
- Repeated the unchanged **entire five-case** browser suite twice more in fresh isolated-fixture runs: **5/5 PASS** on both, including clean global teardown and no skipped/failed cases. Three consecutive complete runs after the synchronization change therefore total **15/15 passing case executions**; this count is repeated runs of the same five tests, not fifteen distinct tests.
- A standalone rerun of the formerly failing unsafe-redirect case had passed on the prior unmodified revision, while repeated adjacent-run failures and the 4.9-second authenticated account trace established the earlier 5-second assertion race. The helper waits on observable HTTP readiness rather than masking errors with retries, ignoring failed responses, or relaxing production security.
- Fourth consecutive full five-case browser run: **5/5 PASS**, with global fixture teardown complete. **Four full runs / 20 successful case executions** at this source state; no retry-based test passing, skips or ignored HTTP failures. The detached Next.js early-stream warnings remain an independently OPEN diagnostic and did not prevent these scoped browser or teardown assertions from succeeding.
- After the browser teardown, unchanged isolated MySQL W04 PHP filter **29/29 PASS (496 assertions)** and API contract PHP filter **23/23 PASS (840 assertions)**; TypeScript check passed before browser runs. These filters and scoped browser acceptance do not claim a full monolithic backend/browser/CI PASS.
