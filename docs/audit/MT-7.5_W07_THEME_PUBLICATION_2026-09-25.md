# MT-7.5 W07 typed Website theme and genuine protected-public publication (25-Sep-2026 LOCAL)

**Scope:** initial independent `20/W07` theme/color/contrast/real Admin/Next checkpoint. Approved source `WebsiteTheme`, `WebsiteSettingRegistry` nine theme values and `WebsiteConfigurationRevisions`; dynamic target theme is separate from immutable static mobiST logo asset files. Parent prior W06 closure SHA `d6cbddf6abd072d41d3d16510bf2e7ca587e88ee` unchanged.

**Implementation:** `WebsiteCms` validates only approved nine six-digit HEX tokens, rejects arbitrary keys/injected CSS and text/surface/background/muted <4.5:1 contrast; previews and revisions remain private until separate publication. Existing partial theme drafts are retained for backward compatibility with accepted W06 behavior; public `/api/v1/content` projects only published theme. Next reads trusted published tokens as CSS custom properties in server root and uses public body background/foreground. Protected Admin offers typed nine-color editor, inline private preview, existing separately permitted version publish and rollback; no source brand/logo binary or live content modification.

**Evidence:** Exclusive localhost synthetic database `mobisttech_test@127.0.0.1:13306` and real Edge `backend/tests/browser-website/theme-w07.spec.ts` **1/1 PASS (1.1m)** terminal `.local/w07_theme_edge3.log`: private draft does not alter actual public style; publication updates public color/body CSS; viewport 390px no horizontal overflow; digital-only, commerce-only, hybrid retain identical published theme; second private revision/publish and protected rollback restore first theme. Exact guarded owner-scoped `W07ThemeE2eCleanupSeeder` and global synthetic cleanup PASS. New PHP `WebsiteThemePublicationTest` joined impacted `WebsiteCmsTest` **10/10 PASS (179 assertions)** `.local/w07_theme_joined_php.log`, includes role ceiling, unsupported CSS/unsafe contrast, draft isolation, publish/rollback API. Backend/Next typechecks and protected Vite build PASS; scoped Pint corrected two new source-style issues. First Edge attempt did NOT pass because `aside` was incorrectly queried as ARIA `region`; second attempt failed only due a test regex requiring a space after React's serialized `style:`; correct semantic locator/whitespace-tolerant regex yielded final Edge PASS. No same-signature retry without material correction.

**Status:** This bounded theme checkpoint is ACCEPTED only; `20/W07` remains OPEN [I,E] for branding role media, media usage/replacement/deletion and safe secret/setting/recovery behavior plus 119-file/15-route bounded source disposition and final family join. MT-7.5 remains 18/27 DONE, 9 OPEN. No W07 stage closure/launch/hosted CI claim.