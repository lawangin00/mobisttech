# MT-7.5 W07: private branded-image publication checkpoint (25-Sep-2026 LOCAL)

Scope: existing stable 20/W07 only. Source WebsiteBranding 7 approved roles map to target typed protected Website presentation revisions, verified private CMS image IDs and fixed mobiST static fallbacks. No legacy media or live brand asset imported; W06 remains DONE; W07 NOT yet closed.

- Protected Admin Website branding editor chooses seven approved roles, explicitly assigns active private JPEG/PNG/WebP; icon/favicon require near-square. Arbitrary role/URL/public disk/video/unknown ID and non-branding role denied by backend; published image route requires exact currently published role and safe private path, caches `no-store`, anonymous wrong-role/foreign/draft/historical URL 404. Actual Next header/footer, favicon/icons and OG image use scoped same-origin proxy with the original approved fixed fallback.
- Private draft, publish, fallback and rollback across real Admin and Next joined at 390px: focused Edge `backend/tests/browser-website/theme-w07.spec.ts -g "W07 protected branding draft"` **1/1 PASS**, terminal `.local/w07_brand_edge.log`, owner-scoped W07Branding -> W07Media -> W07Theme -> global fixture teardown complete. First scoped PHP passed 1/1 (29 assertions); after canonical-role update affected `WebsiteCmsTest`, `WebsiteThemePublicationTest`, `WebsiteMediaLifecycleTest`, `WebsiteBrandingPublicationTest` **13/13 PASS, 234 assertions** `.local/w07_brand_affected_php.log`. Earlier affected 12/13 test error was an outdated `header_logo_media_id` fixture assertion, corrected to new typed `header_logo` and NOT counted as a pass. Vite/TypeScript, PHP syntax, scoped Pint and diff check verified.
- Media deletion now includes all current and historical branding role references in both revisions and materialized published settings; replacement never silently edits image IDs, published URL authorizes only currently selected role. Fixed-original files untouched. The analogous existing W07 theme/media browser PASS is reusable only for previously unchanged portions; exact candidate full-family join remains OPEN.

W07 genuinely pending: Website-specific masked/encrypted secret replacement, wrong-key/rotated-secret configuration recovery and an exact joined W07 family verification with source behavior disposition reconciliation. MT-7.5 index remains 18/27 DONE, 9 OPEN.
