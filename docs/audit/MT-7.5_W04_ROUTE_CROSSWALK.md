# MT-7.5 W04 — immutable Website source-route crosswalk (22-Sep-2026)

**Status: 16/16 SOURCE ROUTES DISPOSITIONED; W04 IN PROGRESS.** This is an exhaustive mapping of the **16 W04-tagged Website source routes**, not of the distinct 52 W04-tagged source files. Source is pinned at Website characterization `04e7c49518f9f11f60c83ad44f9f4e2fd2539066` in `docs/migration/SOURCE_SYMBOL_INVENTORY.json`. The 16 count is a **source** count; target REST/Next routes need not retain the same HTTP path or method. Historical source identities/business records are not imported into the new target. This revision reconciles the original source crosswalk with the later accepted read-only Admin page/API; it does not convert partial read parity into merchant-settings parity.

| # | Immutable source method / URI | Target disposition and evidence | Acceptance boundary |
|---:|---|---|---|
| 01 | `GET\|HEAD admin/payments` | **ADAPT, PARTIAL READ:** protected read-only Inertia `GET /internal/admin/website/payment-settings`, masked `GET /internal/admin/website/payment-channels`, and permission-gated Platform Admin navigation. The separate `GET /internal/admin/platform/payment-destinations` is a different POS payment-destination capability. | Accepted read-only status and navigation are NOT the source merchant configuration, COD bounds, provider-specific settings, history or edit surface. |
| 02 | `PUT admin/payments` | **GAP:** no published, versioned and auditable four-channel merchant/settings mutation equivalent; default-OFF config and a read-only page are not a user-editable source contract. | Owner authority, credential separation, nonsecret setting revisions, publish/rollback and H-02 remain open. |
| 03 | `GET\|HEAD admin/payments/easypaisa` | **ADAPT, PARTIAL READ + HOLD:** the shared Admin status page/API shows masked Easypaisa availability, enabled and merchant-configured booleans, but no provider-specific account settings or encrypted-credential status/history API. | Authentic Easypaisa contract, masked credential lifecycle and settings history remain open. |
| 04 | `PUT admin/payments/easypaisa` | **GAP + HOLD:** no verified Easypaisa-specific protected nonsecret Admin configuration publish flow; fixed registry remains default-OFF. | Provider contract, scope, mode, permissions, audit, version and rollback remain H-02 dependent. |
| 05 | `PUT admin/payments/easypaisa/credentials` | **GAP + HOLD:** no approved owner-bound encrypted, masked Easypaisa credential rotation endpoint. | Owner authority unbound by default; authenticate/reauthenticate, allowlist vendor-approved fields and never copy source secrets. |
| 06 | `GET\|HEAD admin/payments/jazzcash` | **ADAPT, PARTIAL READ + HOLD:** shared Admin status page/API shows masked JazzCash availability, enabled and merchant-configured booleans; provider-specific merchant settings/credential history are not implemented. | Authentic JazzCash contract and masked credential lifecycle remain H-02. |
| 07 | `PUT admin/payments/jazzcash` | **GAP + HOLD:** no verified JazzCash-specific nonsecret Admin configuration publish flow; default-OFF configuration is not account switching. | Provider contract, mode, permission, audit, version and rollback remain open. |
| 08 | `PUT admin/payments/jazzcash/credentials` | **GAP + HOLD:** no approved protected owner-bound encrypted JazzCash credential rotation endpoint. | Real provider contract, explicit owner binding and recent reauthentication remain required. |
| 09 | `POST payments/easypaisa/callback` | **ADAPT, synthetic contract only:** target `POST /api/v1/payment-callbacks/easypaisa` in `backend/routes/api.php` delegates to shared provider verifier and order receipt service. | Provider-authentic signature/amount/reference, transaction and settlement remain **EXTERNAL UNVERIFIED**. |
| 10 | `POST payments/easypaisa/webhook` | **CONSOLIDATE, synthetic contract only:** same target `POST /api/v1/payment-callbacks/easypaisa`; no separate legacy `/webhook` alias proven. | Genuine webhook transport/replay documentation, endpoint compatibility and vendor signed fixtures remain H-02. |
| 11 | `POST payments/jazzcash/callback` | **ADAPT, synthetic contract only:** target `POST /api/v1/payment-callbacks/jazzcash` shares verified-receipt service. | Provider-authentic signatures/references/settlement remain **EXTERNAL UNVERIFIED**. |
| 12 | `POST payments/jazzcash/webhook` | **CONSOLIDATE, synthetic contract only:** same target `POST /api/v1/payment-callbacks/jazzcash`; no separate legacy `/webhook` alias proven. | Genuine vendor webhook transport/replay contract and signed fixtures remain H-02. |
| 13 | `GET\|HEAD project-payment` | **ADAPT, PARTIAL:** authenticated Next `/account/projects` and owned customer `GET /api/v1/projects`; public source reference/token lookup page is not equivalent to an owned account listing. | W05 jointly owns reference/token/identity, access and deep-link disposition; unresolved. |
| 14 | `POST project-payment` | **ADAPT, PARTIAL:** authenticated customer projects listing and selection; no source-style reference lookup POST or historical token migration claimed. | W05 source reference lookup semantics and privacy require disposition. |
| 15 | `GET\|HEAD project-payment/{token}` | **ADAPT, PARTIAL:** Next `/account/projects/{project}` and `GET /api/v1/projects/{project}` use account ownership rather than original token URL. | W05 must decide/document token expiry/link and historical deep-link behavior before parity closure. |
| 16 | `POST project-payment/{token}` | **ADAPT, PARTIAL:** owned customer `POST /api/v1/project-milestones/pay` and `POST /api/v1/payments/{payment}/initiate`; Next `customer-project-portal.tsx` initiates HTTPS hosted continuation. | W05 quote/milestone scope, reference authority, source token behavior and authentic provider acceptance remain separate. |

## Accepted target bounds, without blanket route parity

- `backend/app/Commerce/WebsitePaymentAdministration.php`, `backend/app/Http/Controllers/WebsitePaymentAdministrationController.php` and `backend/routes/website-payment-admin.php`: authenticated, permission-scoped, masked four-channel Admin status and a read-only Inertia page; approved browser navigation and HTTP permission/secret masking evidence are recorded in `MT-7.5_W04_ADMIN_PAYMENT_STATUS_PAGE_ACCEPTANCE_2026-09-22.md` and the subsequent navigation acceptance checkpoint. There is no credential management or activation API.
- `backend/routes/api.php` exposes only three external callback slugs under callback throttle; COD is internal authorized collection, not a callback. `backend/routes/identity.php` exposes owned Website customer checkout, order payment retry/initiation and project milestone routes.
- `website/src/components/customer-project-portal.tsx` and checkout reject non-HTTPS hosted continuation. Synthetic insecure-redirect/recovery case (source commit `c6fcf6393129f2e32ed282d163a26b412aebd418`, full CI `35673288185`) does not activate an authentic wallet/card adapter.
- The provider registry's closed, duplicate-rejecting three-gateway registration, plus its synthetic negative test, are source commits `e6c8062b2cc10416490147506656f5692648e937` and `b6ad42ac907343e33bf8339053c5d4ab029a8f3c`. Their dedicated full-CI result must be separately verified before claiming that checkpoint accepted.

**Still open:** the **52/52 separately tagged source files are individually dispositioned** in `MT-7.5_W04_FILE_CROSSWALK.md`, but GAP/HOLD rows are not functionally closed. Owner-bound merchant credential storage/rotation, publishable nonsecret settings, source negative case mapping, W05 token behavior and genuine provider contracts/signed callbacks/refunds/settlement remain open. Preserve H-02 and do not change 15/27 stage count or close W04 based on source mapping, partial Admin status or synthetic test passes.
