# MT-7.5 W04 — immutable Website source-route crosswalk (22-Sep-2026)

**Status: ROUTE DISPOSITION RECORDED; W04 IN PROGRESS.** This is an exhaustive mapping of the **16 W04-tagged Website source routes**, *not* of the distinct 52 W04-tagged source files. Source is the pinned Website characterization `04e7c49518f9f11f60c83ad44f9f4e2fd2539066` in `docs/migration/SOURCE_SYMBOL_INVENTORY.json`, with source-route family tagging used as the selection criterion. The 16 count is a **source** count; target REST/Next routes need not retain the same HTTP path or method. Historical source identities/business records are not imported into the new target.

| # | Immutable source method / URI | Target disposition and evidence | Acceptance boundary |
|---:|---|---|---|
| 01 | `GET|HEAD admin/payments` | **GAP — admin gateway configuration parity not established.** `backend/config/commerce.php` provides a fixed, default-OFF registry; `/internal/admin/platform/payment-destinations` is a different payment-destination capability, not an equivalent wallet/card merchant configuration screen. | Admin view, four-channel settings/activation and permission/read masking require separate target evidence. |
| 02 | `PUT admin/payments` | **GAP — no proven equivalent publishable four-channel merchant setting mutation.** Target fixed registry and synthetic test registration do not implement the source admin update contract. | Require protected, auditable configuration lifecycle; no synthetic or fake live activation. |
| 03 | `GET|HEAD admin/payments/easypaisa` | **GAP — no verified Easypaisa-specific admin configuration read/masked secret surface.** External Easypaisa remains default-OFF. | Real adapter/credential contract held under H-02. |
| 04 | `PUT admin/payments/easypaisa` | **GAP — no verified Easypaisa-specific admin configuration publish flow.** Default-OFF `backend/config/commerce.php` is not a user-editable provider account. | Authoritative provider configuration, mode, permission, audit and rollback require acceptance. |
| 05 | `PUT admin/payments/easypaisa/credentials` | **GAP — no verified protected, encrypted, masked Easypaisa credential rotation endpoint.** Do not copy source secrets or install fabricated credentials. | H-02 real provider documentation and permissioned credential integration. |
| 06 | `GET|HEAD admin/payments/jazzcash` | **GAP — no verified JazzCash-specific admin configuration read/masked secret surface.** External JazzCash remains default-OFF. | Real adapter/credential contract held under H-02. |
| 07 | `PUT admin/payments/jazzcash` | **GAP — no verified JazzCash-specific admin configuration publish flow.** Default-OFF configuration is not an admin account-switching feature. | Authoritative provider configuration, mode, permission, audit and rollback require acceptance. |
| 08 | `PUT admin/payments/jazzcash/credentials` | **GAP — no verified protected, encrypted, masked JazzCash credential rotation endpoint.** Do not copy source secrets or install fabricated credentials. | H-02 real provider documentation and permissioned credential integration. |
| 09 | `POST payments/easypaisa/callback` | **ADAPT, synthetic contract only:** `POST /api/v1/payment-callbacks/easypaisa` in `backend/routes/api.php` delegates to `WebsiteApiController::paymentCallback` and the provider verifier/order receipt service. | Provider-authentic signature/amount/reference, transaction and bank settlement remain **EXTERNAL UNVERIFIED**. |
| 10 | `POST payments/easypaisa/webhook` | **CONSOLIDATE, synthetic contract only:** same target `POST /api/v1/payment-callbacks/easypaisa`; no separate legacy `/webhook` alias proven. | Genuine provider callback and webhook transport/replay documentation, endpoint compatibility and signed fixtures remain H-02. |
| 11 | `POST payments/jazzcash/callback` | **ADAPT, synthetic contract only:** `POST /api/v1/payment-callbacks/jazzcash` shares the verified-receipt service. | Provider-authentic callback signatures/references/settlement remain **EXTERNAL UNVERIFIED**. |
| 12 | `POST payments/jazzcash/webhook` | **CONSOLIDATE, synthetic contract only:** same target `POST /api/v1/payment-callbacks/jazzcash`; no separate legacy `/webhook` alias proven. | Genuine provider webhook transport/replay contract and signed fixtures remain H-02. |
| 13 | `GET|HEAD project-payment` | **ADAPT, partial:** authenticated Next `/account/projects` and customer `GET /api/v1/projects`; source public reference/token lookup page is not equivalent to an owned account listing. | W05 jointly owns reference/token/identity, access and deep-link disposition; unresolved. |
| 14 | `POST project-payment` | **ADAPT, partial:** authenticated customer `GET /api/v1/projects` + project selection; no source-style reference lookup POST or historical token migration is claimed. | W05 source reference lookup semantics and privacy still require explicit disposition. |
| 15 | `GET|HEAD project-payment/{token}` | **ADAPT, partial:** Next `/account/projects/{project}` and customer `GET /api/v1/projects/{project}` use account ownership, not the original token URL. | W05 must decide/document token expiry/link and historical deep-link behavior before parity closure. |
| 16 | `POST project-payment/{token}` | **ADAPT, partial:** owned customer `POST /api/v1/project-milestones/pay` and `POST /api/v1/payments/{payment}/initiate`; Next `customer-project-portal.tsx` initiates HTTPS hosted continuation. | W05 quote/milestone scope, reference authority, source token behavior and authentic provider acceptance remain independent gates. |

## Verified target bounds (not blanket route parity)

- `backend/routes/api.php` exposes only the three external callback slugs under a callback throttle; COD is an internal authorized collection, not an external callback.
- `backend/routes/identity.php` contains owned Website customer checkout, order payment retry/initiation and project milestone routes. `website/src/components/customer-project-portal.tsx` refuses non-HTTPS continuation URLs.
- The explicit W04 browser negative case on commit `c6fcf6393129f2e32ed282d163a26b412aebd418` and full CI `35673288185` passed the synthetic insecure-redirect/recovery and final disposable schema gates. This does **not** activate an authentic wallet/card adapter or verify any external provider.

**Still open:** individually disposition all 52 separately tagged source files against current target modules (including historical test, CSS, logo and Blade files); verify missing configuration/credential interfaces and negative payment paths. Genuine providers, signed vendor callbacks, refunds and bank settlement remain H-02 HOLD. Do not change 15/27 stage count or mark W04 closed based on this route table.
