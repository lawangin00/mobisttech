# MT-7.5 / W04 — Admin payment-channel status HTTP acceptance (22-Sep-2026)

**Status: W04 IN PROGRESS; 15/27 DONE, 12 OPEN.** This checkpoint supplements `MT-7.5_W04_HOSTED_ACCEPTANCE_2026-09-22.md` and `MT-7.5_W04_ROUTE_CROSSWALK.md`; it does not close the source route parity or authorize any live provider.

## Scoped implementation and acceptance

- `backend/app/Commerce/WebsitePaymentAdministration.php` projects the four-channel status without returning merchant IDs, tokens or encrypted credentials; the existing `website.payments.manage` permission is mandatory. The projection is **read-only** and an `enabled` flag alone never makes an unregistered external adapter available.
- `backend/app/Http/Controllers/WebsitePaymentAdministrationController.php` and the isolated `backend/routes/website-payment-admin.php`, loaded by `backend/bootstrap/app.php`, expose `GET /internal/admin/website/payment-channels` to the authenticated Admin realm with an identity throttle and `private, no-store` caching semantics. They do not add a write, publish, rotate, or activation endpoint.
- `backend/tests/Feature/W04AdminPaymentHttpTest.php` verifies unauthenticated requests are rejected; an Admin having `website.payment-credentials.manage` but not `website.payments.manage` is denied; the permitted Admin receives the masked status and still sees an unregistered synthetic-looking JazzCash channel as unavailable. Test-only secret markers are absent from the response. The test asserts both cache directives regardless of HTTP header serialization order.
- Implementation source commit `84c0382bc3773357d16f6bccec3ccecbfcc7de5d`; assertion-only correction `4afc3adef575e39931ecea366d4cc48521bd0ca4`. Original full run `35680683854` failed **only** because the test assumed a fixed Cache-Control directive order; it is not a passing acceptance run. Exact re-verification source request `441cc60412744374edc586e0ceac741c5d7d8f89` triggered https://github.com/lawangin00/mobisttech/actions/runs/35681116304 . Request validator and full clean-checkout acceptance jobs both completed **success**: PHP style, full backend regression including the new HTTP case, MySQL race/reset, Website TypeScript/lint/build, bundle/secret checks, first-outlet, POS/Admin and Website browser suites, performance, final disposable schema/cleanup and tracked-runtime-artifact gate. Separately scoped W01/P02/W03 isolated browser family steps were skipped in this W04 full request; do not treat those as fresh focused acceptances.

## Remaining OPEN / EXTERNAL HOLD

- Historical `GET admin/payments` now has a **partial read-only target status API**, not equivalent source Admin page/UI or complete configuration/merchant parity. Historical `PUT admin/payments`, the individual JazzCash/Easypaisa edit/credential endpoints, and protected encrypted credential lifecycle, revision/publish/rollback, delegated permissions, audit, masked Admin UI, and HTTP negative cases remain unresolved. The original route crosswalk's full-route acceptance is **not** closed by this checkpoint.
- Genuine JazzCash, Easypaisa and selected hosted-card provider agreements, documentation, authorized sandbox credentials, signed callback/webhook fixtures, settlements and refunds remain **EXTERNAL UNVERIFIED / HOLD**. Do not activate fake adapters, invent credentials, store raw PAN/CVV, weaken signature or final schema gates, or equate synthetic tests with genuine provider acceptance.
