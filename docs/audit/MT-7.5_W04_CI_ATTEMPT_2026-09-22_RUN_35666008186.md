# MT-7.5 W04 CI attempt: 35666008186 (22-Sep-2026)

Status: IN PROGRESS / acceptance NOT complete. Target repository `lawangin00/mobisttech`; explicit necessary CI request commit `12ac3dc74128dd280329aa225969cab50f3b10ac`, tested selector-correction parent `ac488515b2ad4b209b410b96c4d52b3730bcb94c` (one test-only locator change, no production payment behavior changed). Previous full browser attempt `35660464986` failed on ambiguous checkout `getByRole('alert')` matching Next.js route announcer as well as expected payment error. This attempt materially changed that selector and passed the checkout assertion; it is NOT an equivalent blind retry.

Verified terminal outcome of `35666008186`, job `106551837163`: FAILED only at final `php ../tools/migration/verify_mysql_schema.php api` with `FAIL: Unexpected business rows remain in disposable schema.` Earlier full backend regression PASS (341 tests, 10059 assertions); explicit race/reset PASS (23 tests, 1684 assertions); POS/Admin Playwright PASS (33 passed, 1 intentional skip); Website checkout PASS (3); customer/project PASS (5); public content PASS (5); website performance PASS (1). Exact independent website static workflow `35666008081` PASS. None of these individual gate passes constitutes full terminal clean-checkout acceptance.

The POS/Admin global-teardown CI diagnostic printed `CI_DISPOSABLE_RESIDUAL_TABLES=identity_audit_events:1,publication_versions:3` before subsequent Website suites. This does not prove that these are the only offending final rows: final schema verifier reports no table name and website suites run later. Do not attribute the single audit row or three publication version rows to a specific fixture without a further narrowly scoped, counts-only diagnostic. `tools/migration/verify_mysql_schema.php` requires exact baseline business-row counts; do not waive or weaken this guard, bulk-delete unknown data, or rerun the same unmodified CI. First genuinely pending action: measure final residual table/count delta after Website global teardown in disposable CI schema, compare to schema verifier's expected seed counts, identify owned fixture source, then make scoped guarded cleanup correction and verify it with a materially different targeted test before any new full hosted CI request. Preserve 52-source-file / 16-source-route W04 parity disposition, external-provider authenticity HOLD, and MT-7.5 15/27 DONE 12 OPEN status until independently evidenced.

Attempt tally for the checkout selector subtask: earlier ambiguous-selector attempt 8 = failure signature `NEXT_ROUTE_ANNOUNCER_ALERT_COLLISION`, selector-only material correction = verified browser PASS in attempt 9; current separate workflow failure signature `FINAL_SCHEMA_BUSINESS_ROW_RESIDUE` = first observed terminal failure on the corrected source. Do not conflate these distinct failure signatures or count a future unchanged cleanup rerun as a new strategy.
