# MT-7.5 W06 four bounded development actions: global SEO, social media and internal security (25-Sep-2026; LOCAL)

Parent `575691e82138c61d090d14059fff1d39090b359f`; stable gate `19/W06`. No authentic provider, production, protected source, cross-project, or hosted CI actions. All test fixtures are synthetic on locked `mobisttech_test@127.0.0.1:13306` and have named cleanup owners. This is not W06 family closure.

1. **Editable global Website SEO.** An independently permission-scoped protected Admin form drafts global title/description/social title/social description and a root-only canonical; CMS rejects unknown fields and foreign/external canonical values, stores versioned publication snapshots and separately requires `website.publish`. Public index/Next root and homepage metadata use only the published values, with independent per-product metadata unchanged. Direct real Admin -> public draft-isolation/publish test passed in the preceding focused 4/4 (the command-line grep unintentionally selected four tests rather than one); exact candidate full dynamic-content Edge 7/7 PASS below.
2. **Managed-page social media.** New Laravel `/api/v1/content/pages/{slug}/media/{id}` only reads a mode-allowed published page's own active image reference with constrained MIME/UUID path. Same-origin Next `/[slug]/media/[media]` serves only these bytes; actual Admin synthetic image upload -> private draft (404) -> published social OG/Twitter image and HTTP 200, with cross-product/draft/unknown denial. Owner-verified image cleanup occurs after the fixture page deletion. The independent managed-page focused Edge test PASS.
3. **Collision, ownership, role and cache checks.** Reserved actual Next application roots cannot be created as managed CMS pages; protected route check remains distinct from valid promotional links. Isolated feature tests confirm `website.seo.manage` cannot author navigation/publish, canonical/title sanitization, draft isolation, published data change and rollback, page-media ownership/retirement and no public-disk file. Scoped derived cache uses an ephemeral array store inside DBTransaction-based feature tests: failed initial isolated assertion was traced to persisted file-cache keys from rolled-back, reused test version numbers, not accepted as product PASS; changed the test cache isolation, not production cache policy. Updated PHP CMS family **8/8 PASS, 131 assertions** on final candidate.
4. **Private-at-rest media hardening.** CMS uploads now use local private (non-web-served) storage and private visibility, rather than Laravel's symlink-exposed `public` disk. Deduplication does not reuse older public-disk assets. Both Software and managed page routes fail closed for assets not on private disk, and synthetic teardown deletes only fingerprinted images from that disk. Earlier public-disk CMS rows/files, if any exist outside this fresh test schema, were NOT migrated/deleted or treated as launch-safe; inventory and owner-authorized migration/remediation are needed before exposing any such deployment. POS branding storage outside CMS was not modified.

Final verification: backend Admin Vite build and Website Next production build/typechecks PASS, affected PHP syntax and scoped Pint PASS, candidate full real Next/Admin `dynamic-content.spec.ts` **7/7 PASS (3.2m)** with guarded DB teardown; after the last private-disk/dedup change, impacted Software media browser **1/1 PASS (1.2m)** and managed-page media browser **1/1 PASS (42.2s)**, final CMS PHP **8/8 PASS**. Every failed diagnostic/test run above was superseded by a materially corrected terminal PASS, never counted as evidence. Project policy and Git whitespace guards PASS.

**Gate disposition:** W06 remains OPEN [E, conditional I]; frozen 72/72 source files and 45/45 routes already have target/retirement dispositions but those mapping totals alone are not behavior acceptance. Next independently executable work: bound the second-product version/rollback/archive/route collision and unpublished/role/mode/cache closure assertions to genuine gaps against existing evidence, then perform the single applicable W06 family closure reconciliation; do not declare 18/27 before all mandatory gates are satisfied. Current development progress stays **17/27 DONE, 10 OPEN**.
