# MT-7.5 W04 — All four Website payment channels unavailable: scoped browser acceptance (23-Sep-2026 PKT)

In a single real local Playwright browser run, an authenticated synthetic customer added a product to cart, visited checkout and received a browser-only mocked payment-channel response with all four approved channels unavailable. All four radio options remained visible and disabled, Place order was disabled, and no order POST was attempted. This is a UI-state test only: it does not alter the test database's COD policy or activate a real provider. Separately, `MT-7.5_W04_COD_OFF_CHECKOUT_HTTP_2026-09-23.md` independently verifies the actual backend COD-off policy rejects new authenticated orders without persistence.

`npx playwright test --config=playwright.website.config.ts tests/browser-website/checkout.spec.ts --grep=all.unavailable --reporter=line`: **1/1 PASS** (46.7s), global setup and scoped fixture teardown completed, process exit 0. The Next server printed nonfatal `destination stream closed early` notices; no order, test failure or server readiness failure was observed in this focused run. Backend TypeScript `npm run typecheck` PASS; website `npm run lint`, `npm run typecheck`, `npm run build` PASS (lint emitted one unrelated existing `<img>` performance warning, 0 errors). W04 remains IN PROGRESS, 15/27 DONE / 12 OPEN; H-02 genuine external provider/refund/settlement HOLD unchanged.
