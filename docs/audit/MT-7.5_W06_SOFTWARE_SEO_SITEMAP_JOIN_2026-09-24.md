# MT-7.5 W06 ? Software SEO / sitemap real protected-to-public checkpoint (24-Sep-2026; LOCAL)

Scope: existing `19/W06` only, candidate based on `bd5b9128d4306b223987b254e3596c556a5b19ba`. Genuine protected Admin UI now exposes validated per-Software optional SEO title, description, canonical URL, social title/description, active social-image asset selection and sitemap setting, loading the selected revision rather than silently replacing SEO on save. Published Next Software Overview uses published backend SEO title/description/canonical/Open Graph/Twitter fields and safe product-identity fallback. The backend public content-index now projects approved published `sitemap` boolean, and Next sitemap omits Software route families when the current published revision has `sitemap=false` without deleting or 404ing its historical public release pages. The second product does not overwrite the independently published first product. These are internal development actions, NOT merchant/production activation or complete W06 parity.

Verification: initial focused Edge attempt timed out at missing SEO placeholder because the protected Admin served previously built Vite assets; root verified by no new-string match in existing public/build and corrected by `npm --prefix backend run build` (typecheck/Vite exit 0). The next focused attempt found a stale test assertion expecting product-name `<title>` despite intentional SEO override; corrected its exact expectation. The subsequent attempt scoped metadata lookup to `head`; actual Next 16 streamed metadata into `body`, confirmed with retained Playwright trace showing real published SEO description/canonical/OG/Twitter attributes under `/HTML/BODY`, so corrected the test strategy to location-independent DOM metadata selectors. These are distinct environment/test-orchestration and stale-assertion signatures, not masked product defects or fake PASS; no unchanged failing command was rerun. After material correction the real focused W06 Edge test **1/1 PASS (1.1m)**; affected sequential full `dynamic-content.spec.ts` Edge **4/4 PASS (1.8m)**, including prior content/mode and Admin publish/rollback neighbors. Isolated `ApiContractTest` two affected PHP tests **2/2 PASS (50 assertions)**. Scoped PHP syntax/Pint, both TS typechecks, protected Admin Vite build, local Next production build through Playwright and diff whitespace PASS. All browser runs used the cooperative exclusive `mobisttech_test@127.0.0.1:13306` wrapper, with exact W06 fixture/global teardown; no fresh empty-schema or all-project Q01 acceptance is claimed. Repeated Next server `destination stream closed early` messages are nonterminal internal logs; isolated and joined browser suites both returned exit 0.

Remaining `19/W06` non-HOLD work: actual social image/media serving and per-product safely rendered images (stored media IDs are **not** a public URL), source CMS hierarchy/navigation/banner/protected page collision and editable global/managed-page SEO, two-product update/rollback/slug/archive/role/security and responsive/performance/cache closure. Continue first genuine missing internal implementation/verification, not already accepted W04/W05 or the 72/45 disposition. Owner-approved real Software/legal/brand content, H-02 actual providers and live activation remain separately PRE-LAUNCH PENDING; payment channels OFF. W06 OPEN [I,E], MT-7.5 **17/27 DONE, 10 OPEN**, Q01 exact-current-candidate/clean-schema OPEN, no launch readiness or 100% claim.
