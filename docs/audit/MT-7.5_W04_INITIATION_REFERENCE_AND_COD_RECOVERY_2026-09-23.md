# MT-7.5 / W04 — initiation reference boundary and malformed published COD recovery (23-Sep-2026)

- Test-first showed a synthetic vendor reference of 256 characters was rejected by MySQL `gateway_order_reference` truncation error rather than the domain contract. The initiation boundary now rejects blank or >255-byte vendor references as a `LogicException` before binding; the existing pending payment remains unbound, with no receipt or sale fabricated.
- Test-first showed a published COD snapshot with string `cod_enabled: "true"` threw from `codEnabled()` and broke authorized Admin settings visibility. Invalid published policy now fails closed as COD-off, exposes `published_invalid` on the permission-gated Admin page, and permits explicitly creating and separately publishing a valid recovery draft. An unpublished draft never enables checkout.
- Authenticated Admin HTTP regression proves the invalid state is viewable without checkout exposure, and authorized draft/publish restores a valid state; the Admin UI displays a nonsecret warning. Real gateway configuration and merchant credentials remain untouched.
- Focused initiation reference boundary regressions 2/2 PASS (9 assertions), including an accepted 255-byte reference and cached continuation with one provider call; published-policy service+Admin HTTP regressions PASS. Joined W04/OrderPayment/ApiContract 89/89 PASS (1627 assertions); expanded W04/OrderPayment/ApiContract/CustomerApi/Website/Commerce 103/103 PASS (1806 assertions); Website checkout browser 6/6 PASS, existing Admin COD browser 2/2 PASS with disposable cleanup, scoped Pint and TypeScript/Vite build PASS.
- W04 remains IN PROGRESS, MT-7.5 remains 15/27 DONE / 12 OPEN; authentic provider contract, disabled-channel receipt policy, credential lifecycle, refunds and settlement remain H-02 HOLD. These are synthetic acceptance gates, not live-money verification.
