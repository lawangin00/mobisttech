# MT-7.5 - Deterministic parity closure / retry control

Status: IN PROGRESS. This is a gate index, not a claim that test-file existence proves parity. Source of authority: `docs/migration/FEATURE_PARITY_REGISTER.md`, current Goal/Source of Truth/approved requirements, `docs/audit/MT-7.5_TRACEABILITY.md`, and exact test evidence in `docs/audit/MT-7.5_ACCEPTANCE.md`. Do not reopen completed roadmap points to restate their previously accepted result.

## Execution and closure rule

For each row below, compare the *original source behavior* with current approved superseding behavior, execute independently observable target backend/API/UI tests, record the precise passed/failed assertion and SHA, and distinguish missing functionality from missing proof. Only then change that family's target status. Never infer production approval from synthetic fixtures. `HOLD` is not a PASS. No family is closed by unrelated historical CI or a standalone new test. A repeated failure invokes inherited Universal Registry 1.35 `LOOP_GUARD`: record signature/attempt/change; same failure twice or 3 unsuccessful attempts blocks equivalent retries even on `Proceed`.

## Complete source-family inventory (22 of 22; canonical order)

| ID | Current source-register status | Specific remaining acceptance / next resolution |
|---|---|---|
| P01 | Pending | Reconcile original three-guard source contract against *approved* unified Admin/Customer realms; verify NEW Admin and Customer provision, NEW outlet setup, cross-role/outlet direct denial, password reset/session invalidation and actual UI; OLD source identity/collision mapping is not a gate. See identity decision below. |
| P02 | Complete | MT-7.5 P02 closure: 18/18 tagged route dispositions; protected category/variant and managed-option histories, source-qualified synthetic imported-product real Edge edit, product ID/brand/stock preserved; changed-candidate backend 18/18 (208 assertions), imported Edge 1/1 PASS. P03/P06/W02 functional gates independent; legacy data cutover excluded. |
| P03 | Complete | All 12 source routes disposed; protected acquisition/movement history, quantity/physical stock, required IMEI slots and historical/current claims, receiving/reservation/sale/sellable-return totals, rollback/concurrency and outlet/role scope accepted in `MT-7.5_P03_STOCK_PARITY.md`. |
| P04 | Complete | Eight source routes disposed; negative sale/return, original invoice/customer/business/product snapshots, precise money/stock/payment effects, cross-outlet history, invoice bindings and permissions accepted. |
| P05 | Complete | Six source routes disposed; inclusive expiry, claim lifecycle/concurrency, clause-version snapshot, role/outlet denial and historical A4 warranty receipt accepted. |
| P06 | Complete | All 36 routes disposed; immutable A4/Thermal actions, safe Email/assisted WhatsApp history, date/category/profit/CSV reports and day-close/payment mix accepted. Authentic Gmail remains external unverified. |
| P07 | Complete | All 148 source files and 32 routes disposed; settings/media permissions, draft/publish/rollback, safe paths, responsive presentation, runtime theme/branding and invoice/warranty document logos accepted in backend and actual Edge. |
| P08 | Complete | Preserve MT-3.3/MT-7.1/MT-7.4 target-only verified recovery evidence. Live destructive restore/provider work remains a separate authorization HOLD; do not reopen this completed family without regression evidence. |
| X01 | Complete | All 11 source routes disposed; current payment regression and independent-connection confirmation/release race verify replay/hash conflict, atomic retry, expiry and joined order-stock-payment reconciliation. |
| W01 | Complete | Reconciled original customer/Admin identity and signed historical order access, password/profile/avatar/session, last-owner and direct cross-realm denial with customer and Admin browsers. Authentic recovery delivery remains separately external-unverified. |
| W02 | Pending | Check source category/filter/price/variant/redirect and zero-stock outputs against live shared POS→Laravel API→Next.js propagation; deny private fields. |
| W03 | Pending | Join guest-cart to customer recovery, reprice/duplicate checkout, signed owned invoice/order, purchase-qualified review moderation and Admin CSV/status UI. |
| W04 | Complete (development only) | Existing fixed four Website-only channels, API/Admin/UI, negative owner/reference/replay/amount/refund/disabled-provider and synthetic provider contract close via `MT-7.5_W04_DEVELOPMENT_ACCEPTANCE_CHECKLIST_2026-09-24.md`: isolated PHP 117/117 (1,913 assertions), real Admin Edge 5/5 and Website checkout Edge 7/7, synthetic teardown PASS. Genuine gateway merchant-specific sandbox/refund/settlement and live activation remain enforceable PRE-LAUNCH H-02 PENDING, NEVER synthetic PASS. |
| W05 | Complete (development only) | All 24/24 source files and nine routes disposed. Real public enquiry -> unmocked Admin proposal/status/private delivery -> Customer; signed-in foreign/anonymous project/file denial across three modes, exact milestone/expiry/refund-adjacent privacy, paid-only completion and closed history accepted in Edge 7/7 + isolated service PHP 10/10. Genuine provider H-02 remains PRE-LAUNCH PENDING and the distinct Q01 zero-residue full-candidate gate remains OPEN. See `MT-7.5_W05_DEVELOPMENT_CLOSURE_2026-09-24.md`. |
| W06 | Pending | Source CMS/nav/SEO, safe media, protected collisions/rollback and cache; real joined Admin→Next.js Software Overview revision/rollback locally passes, but new-product create, complete public route family and media/SEO do not yet have equivalent browser closure. |
| W07 | Pending | Compare theme/settings/media revision recovery, secret masking/key mismatch, safe replacement/deletion, responsive layout and current-source behaviors in real protected/public UI. |
| W08 | Complete | Preserve MT-3.3/MT-7.4 integration health/job readiness. Real provider activation remains separately authorized HOLD. |
| B01 | Complete | Preserve independently completed MT-6.1 brand master/runtime proof; only a demonstrated regression can reopen. |
| C01 | Complete | Preserve independently completed MT-6.3 Windows Control ownership/launcher acceptance; no unrelated host/source changes. |
| Q01 | In Progress | Final same-candidate clean checkout plus all family/cross-requirement closure; current GitHub runner prestart billing rejection makes later candidate CI unaccepted. |
| F01 | Complete | Preserve MT-7.3 shared schema/build/CI foundation; exact current release-candidate verification belongs to Q01, not reopening F01. |
| H01 | Pending | Complete original source-file/route/behavior-to-target disposition and verify historical docs cannot override approved new instructions; evidence must be exhaustive, not file-name presence. |

Inventory invariant: five previously Complete (`P08,W08,B01,C01,F01`), sixteen Pending and one In Progress (`Q01`). These are **family** counts, not roadmap-point counts. Never mass-convert Pending solely because its component roadmap owners finished.

## Approved requirement groups (separate from source-family rows)

| Requirement authority | Required closure evidence / unresolved decision |
|---|---|
| Addendum v1.1 | All **41** clauses individually reconciled against target backend/API, rendered role/mode UI and applicable performance/reset tests; current clause-to-roadmap-owner table is not final acceptance. |
| Consolidated documents/payments/legal/manual v1.0 | Invoice/Warranty explicit customer actions, split POS methods/destinations/day close/payment mix and distinct Website four channels; actual legal/ownership/notice decisions. Final manual belongs to MT-7.6, not premature MT-7.5 completion. |
| Software publishing v1.0 | Create **new** synthetic product via authenticated UI, private full-route preview and all public Overview/Privacy/Terms/FAQ/Releases/detail, revision/update/rollback, multi-product isolation, safe media/SEO/caches, documentation-impact review and responsive performance. Existing seed/product journey is partial evidence only. |
| mobiST POS reference set | Verify four reference documents against separately released **Windows desktop** POS and approval of factual product-specific terms/data flows before public publication; browser POS is not this product. |
| Legal/privacy/license | Final version/effective-date, actual owner/legal sign-off, third-party notices and truthful real-world data flows; synthetic `owner_approved` fixture must never substitute for explicit approval. |

## W01 expiring signed historical-order checkpoint (21-Sep-2026; partial)

- Owner-authenticated order detail now issues a 30-minute signed, read-only historical-order summary capability and exposes it in the Customer order UI. The endpoint is private/no-store; tampered, expired and mutating requests fail, and another Customer cannot mint a link for an unowned order. Focused **1/1 PASS (13 assertions)**; joined identity/session/mode **19/19 PASS (177 assertions)**; Website typecheck PASS.
- This closes the missing signed-capability implementation slice only. W01 remains Pending until actual Customer browser link/expiry coverage, Customer profile/avatar parity and joined last-owner/direct cross-realm browser acceptance pass. Authentic recovery delivery remains an external HOLD and old identity cutover is excluded.

## W01 family closure (21-Sep-2026)

- Hosted exact-source run `35619045788` passed the final clean-checkout identity browser join. Chromium Admin **1/1 PASS (9.7s)** covered protected-owner self-authority denial plus direct Customer-realm denial; Chromium Customer **1/1 PASS (11.7s)** covered signed history/profile/photo plus direct Admin-realm denial.
- Existing current evidence covers independent Admin/Customer guards, password/session revocation, profile/photo privacy, protected Full Access/last-owner behavior and local/mock recovery behavior. Final schema, cleanup and tracked-artifact gates passed.
- W01 is Complete. Authentic provider delivery and old identity import/cutover remain separately external-unverified or excluded and do not reopen the accepted target capability family.
- Customer-owned name/mobile profile and private self-scoped photo CRUD are now implemented with strict validation, atomic linked-customer projection, UUID private paths, safe media replacement/removal and audit. Focused **1/1 PASS (14 assertions)**; joined identity/session **17/17 PASS (140 assertions)**; Website typecheck/build PASS. Direct Edge accepted signed historical access and profile edit/persistence/restore; the separately recorded isolated hosted Customer browser passed signed order/profile/photo **1/1 (12.1s)**. Final last-owner/cross-realm browser join remains Pending; the hosted workflow's independent stale schema-count failure is not an overall CI PASS.

## First evidence-backed discrepancy: P01 contract supersession, not permission to revert identity

The original source characterization describes three POS guards (`FEATURE_PARITY_REGISTER.md` P01). The newer binding `PROJECT_REQUIREMENTS_UNIFIED_ADMIN_GOOGLE_v1.0.md` and `PROJECT_REQUIREMENTS_TEAM_MEMBERS_SESSION_POLICY_v1.0.md` supersede that credential architecture: internal Team Members share one Admin realm and customers have a distinct Customer realm (`PROJECT_SOURCE_OF_TRUTH.md`); `TeamMemberSessionSecurityTest::test_only_admin_and_customer_realms_exist_and_default_roles_are_permission_bundles` asserts the approved two-realm design. Therefore **do not** blindly recreate a third login guard or mark P01 Complete from old source guard count. Pending P01 acceptance is a source-role/guard → current Admin role+outlet+permission mapping with cross-role, cross-outlet, reset/revocation and NEW Admin/Customer/outlet login, permission and cross-realm target evidence. Old-business identity migration is EXCLUDED. MT-2.18/MT-2.19 remain completed; this is MT-7.5 traceability only. Synthetic MT-7.1 rehearsal proves mapper behavior, not unauthorized live source-identity migration.

## Existing failed-task ledger and retry barrier

- Software joined-browser test: asynchronous Admin reload and synthetic rollback-reference cleanup were distinct local failure signatures; corrected test and scoped cleanup passed actual Edge 3/3 with both teardowns. No active equivalent retry is needed; reuse the proven test runner/fixture, do not restart discovery.
- GitHub CI task: run `35442617990` at SHA `21af94a6` failed **before** a runner or any job step started because GitHub reported failed account payment or Actions spending-limit exhaustion. This is one recorded infrastructure failure, **not** product test failure. Do not dispatch equivalent CI merely for a new commit/`Proceed`; retry only on verified material billing/runner-readiness change after account-holder action. Its outstanding exact-current-candidate CI proof remains OPEN.
- On any *future* failure, before retry record logical task, attempt number, source/harness/environment identity, terminal outcome/signature and material corrective change in `PROJECT_IMPLEMENTATION_STATUS.md`. Same equivalent failure twice or three unsuccessful/incomplete attempts: `LOOP_GUARD` active, audit whole affected workflow, change strategy and preflight before a new attempt. Missing terminal evidence counts as `ORCHESTRATION_FAIL`.

**Next bounded parity action:** audit P01's approved source→target identity mapping and exact authenticated cross-role/cross-outlet + reset/session journeys against the original source contract. Record a concrete missing implementation versus evidence-only gap; do not add an arbitrary isolated assertion, start MT-7.6, change owner/legal states or repeat billing-blocked CI.

## P01 focused verification checkpoint (19-Sep-2026; target-only)

- Fresh isolated `mobisttech_test`: `IdentityMigrationTest` + `IdentitySecurityTest` + `TeamMemberSessionSecurityTest` **22/22 PASS (162 assertions)**. This joins source-qualified identity mapping/quarantine/replay, unified Admin/Customer permission and reset/session safety.
- Fresh real local Edge `backend/tests/browser/pos-shell.spec.ts` **2/2 PASS**, verifying desktop salesperson permission-scoped navigation and direct denied inventory route plus mobile inventory operator outlet selection, denied sales route and logout. POS E2E synthetic seeding/cleanup succeeded. Test-owned MySQL returned to its initially stopped state after *each* test run.
- P01 remains **Pending**, not a failed implementation: the original source register's full 54-route/three-guard behavior-to-approved-role mapping and separately authorized real source identity cutover remain unaccepted. Next P01 action is **source-route/role-to-target contract crosswalk**, *not* another equivalent rerun of these already-PASS tests. Two Admin/Customer realms are the approved replacement, not a regression requiring a third guard.

## P01 original-route reconciliation (19-Sep-2026; exact source inventory)

- `docs/audit/MT-7.5_P01_ROUTE_CROSSWALK.md` now enumerates **all 54 of 54 P01 original POS routes**, each with method, URI, source controller action, source middleware/permission, target approved equivalence or gap, and disposition. It compares read-only source controller behavior with current two-realm authority; the legacy three guards/shared Shop credentials and GET logout are intentionally superseded and must not be resurrected.
- **Proven implementation gaps, not missing tests:** assigned-outlet profile and authorized outlet lifecycle (6 source routes); personal Admin/SuperAdmin profile photo upload/removal (4); Admin account/password/recovery **browser** workflow despite existing password APIs (3 account entry routes plus password routes); read-only scoped POS audit viewer (1); saved POS list/search preferences (2). These are NOT marked PASS. Original operators' invoice/claim attribution, team delegation, business-profile canonicalization and report scope have candidate target anchors but retain their independent family acceptance where noted.
- **Next non-redundant implementation:** first restore an authorized, assigned-outlet profile and owner-controlled outlet lifecycle using the existing unified Admin permissions/outlet identity, immutable `OUTLET3`, protected history and recent-auth where applicable; add focused direct-denial and actual UI acceptance before marking those six routes resolved. Do not rerun the already-PASS P01 test suite unchanged; do not infer real owner/legal/provider decisions or bypass GitHub's billing pre-run block. P01 remains Pending and MT-7.5 remains In Progress; MT-7.6 unstarted.

## P01 assigned outlet profile implementation (19-Sep-2026; partial)

- Added protected POS `profile` workspace and selected-outlet-only `GET/PATCH /internal/admin/pos/outlet-profile`. A `shop.profile` + `shops.enter` holder can edit name, legal name, phone/WhatsApp, address and business hours of **only the currently selected, assigned, active** outlet. Explicit server-side `Access` rechecks and row lock, immutable outlet code, optimistic `version` conflict, input allowlist, normalized Pakistani phones, per-actor/outlet identity audit; no new credential realm, source mutation, outlet archive or historical deletion. Outlet-code generation, outlet create/archive and existing business-identifiers editor remain separate unclosed P01 gaps.
- New isolated MySQL `PosShellTest` focused **1/1 PASS (24 assertions)**, including malicious outlet-code/ID edits, invalid phone, stale version, assigned-versus-other outlet, permission denial and archived outlet. Test database restored to initially stopped state. Backend PHP syntax PASS; frontend typecheck and rebuilt Vite production bundle PASS.
- **Browser Loop-Guard ACTIVE (one logical new outlet-profile UI test, two same-class unsuccessful attempts).** Attempt 1: Playwright strict locator `getByRole('link',{name:'Outlet profile'})` matched both desktop navigation and Home card. Material change attempt 2: `getByTestId('nav-profile')` still matched *both* mobile and desktop `Navigation` elements rendered concurrently; both attempts stopped at the first assertion before the form was exercised. Both ended with successful synthetic cleanup and restoration of initially stopped MySQL. This is an ambiguous test-selector/harness failure; not evidence of a broken outlet update API.
- Comprehensive selector sweep established POS shell always renders `<Navigation mobile />` and desktop `<Navigation />`; all new form testids are in the single outlet profile component. Corrected the test to target `page.locator('aside').getByTestId('nav-profile')`, the actual desktop sidebar, while retaining direct HTTP 200, editable form, response and persistent reload checks. This is an evidence-based different locator scope, **not yet a browser PASS**. Do not repeat an equivalent unscoped locator or declare P01 closed. First pending step: verify the corrected new test once after preflight, then targeted sibling browser and backend regression if PASS; on a new failure classify its signature and obey the active Loop-Guard. GitHub clean-checkout remains separately billing-blocked; no unchanged CI attempt authorized.

## P01 outlet UI and guarded lifecycle (19-Sep-2026; bounded verification)

- Corrected previously twice-failed ambiguous profile locator was verified **once** with the independently identified desktop `<aside>` scope: real local Edge **1/1 PASS (17.4s)**, POST/PATCH response and post-reload profile persisted; seeded fixtures cleaned and test MySQL restored to its initially stopped state. Previous `strict mode violation` signature no longer present; already-PASS test was not repeated again unchanged.
- Added protected Full Access-only outlet catalogue/create/empty-only archive, each mutating route requiring recent password confirmation. One Admin account remains the sole personnel credential; new outlet receives a unique unused numeric `001`-`999` code, immutable business/public identity, no outlet password and only creator assignment. Existing and archived codes are never recycled. Scoped existing-outlet editor remains separate and unchanged.
- Archival is intentionally **fail-closed** on any outlet-linked business row (including supplier, stock, sales, reservations, documents) and last-open-outlet guard; audit and Team Member identity attribution are retained. Active/historical outlet transition must be separately reviewed; no automatic data removal or blind historical archive. Owner UI exposes only existing enabled empty-outlet archive and asks for password again on mutation.
- Focused authenticated target-only MySQL `PosShellTest::test_protected_full_access_outlet_create_archive_and_direct_permission_barriers` **1/1 PASS, 27 assertions**, including unprotected permission spoof denial, invalid code injection, stale revision, audit, archived code non-reuse and linked supplier business-row archive 409. Rebuilt frontend typecheck/Vite build PASS. Actual Edge protected Admin create/archive + limited operator direct route denial **1/1 PASS (30.6s)** with synthetic cleanup and prior MySQL stopped-state restoration.
- New browser attempt 1 returned page HTTP 200 but no page content because the explicit Inertia `resources/js/app.tsx` resolver lacked the newly authored `outlet-management` page. Material fix: register component in exact page resolver and rebuild (`581` modules vs previous `580`); attempt 2 PASS. Different root cause from previously closed selector signature; no equivalent third/failing retry. All three tests are **local only** on this new unpushed candidate; GitHub billing pre-run blocker remains unchanged, so do not dispatch equivalent remote CI. P01 still Pending (unassigned-owner edit, historical archive policy, Admin profile/photo, recovery/account, audit viewer and portal preferences), MT-7.5 In Progress, MT-7.6 unstarted.

## P01 Admin account and gated recovery interface (19-Sep-2026; bounded local acceptance)

- The approved **single Admin realm** now has protected `GET /internal/admin/manage-account` with identity/role display, current-password PATCH form, and links from POS and Platform Admin. The existing backend password mutation revokes previous sessions and requires re-login; customer/shop/superadmin credential realms are NOT added. An anonymous forgot-password page and the real Gmail-generated `/internal/admin/reset-password?token=...&email=...` landing route use existing Admin broker/API, do not project tokens to Inertia props and send no-store/no-referrer headers. The Admin session heartbeat excludes these two intentionally public recovery forms.
- Recovery send remains **OFF** unless approved delivery flag **and** connected Gmail are both present; no real email/provider operation was performed. A delivery-disabled request returns 503 and the UI explains recovery is unavailable, not falsely 'sent'. Owner one-time offline recovery and unconfigured provider decisions are NOT resolved by these forms.
- Focused target-only `PosShellTest::test_account_and_recovery_pages_use_only_the_existing_admin_realm`: **1/1 PASS (40 assertions)**, including anonymous account denial, public recovery route/headers, no reset token in component props, disabled delivery 503 and authenticated scoped account rendering. Frontend TypeScript/Vite build PASS; focused real Edge Admin account/password change and invalid old credential/new credential login **1/1 PASS (24.8s)** before adding an extra disabled-delivery browser assertion; owned MySQL restored to initially stopped state.
- **LOOP_GUARD diagnosis:** the focused backend test first exposed invalid `Inertia\\Response::header` (fixed by converting to a real Symfony response before setting headers); next exposed an overly exact cache header test (`no-store` vs valid Laravel `no-store, private`, fixed to assert the `no-store` directive). After distinct fixes, backend PASS. First focused browser test expected a source-level current-password error string, whereas the canonical Identity exception normalizes 422 to a generic public error; corrected assertion and focused browser PASS. A later materially expanded joined 5-case browser run passed the first four but hit Admin login 403 in case 5 because case 4's separate negative-permission browser context left an active synthetic session for the same account (verified against Playwright network trace and POS device session limit). Case 4 now explicitly signs out its synthetic operator before destroying that context; no change to product device/session policy and no equivalent retry without this lifecycle correction. After explicit test-owned guest operator logout and a verified different fixture lifecycle, full joined real Edge POS shell **5/5 PASS (1.5m)**. Focused backend full `PosShellTest` **6/6 PASS (173 assertions)**. Both synthetic teardown and target-only MySQL initially stopped-state restoration PASS. Browser guard is no longer retrying the previous 403 signature; do not repeat unchanged suites.
- P01 remains Pending: read-only scoped POS audit viewer, saved legacy list/search preferences, owner unassigned-outlet editing/historical archive HOLD, real source identity cutover, email-provider delivery and GitHub Actions billing-blocked exact-candidate CI remain separately open. MT-7.5 In Progress; MT-7.6 unstarted.

## P01 Admin personal-photo parity (19-Sep-2026; local, self-only)

- Existing Admin/SuperAdmin source photo POST/DELETE endpoints are reconciled into **one approved Admin-realm personal photo** at `/internal/admin/manage-account/photo` (authenticated GET/POST/DELETE) plus the actual My Account React form. The original separate SuperAdmin guard and publicly writable source directories are NOT revived; `admin_id` or other subject overrides are rejected. Only the signed-in Admin can read/update/remove that Admin's photo. JPG/PNG/WebP <=5 MB, real image validation, random UUID file name under an account-specific **private** disk prefix, `no-store`/`nosniff` private streaming, DB row-lock, identity audit and prior managed-object removal. Pre-existing legacy filenames are not silently read from the source/public location or deleted; private media re-import needs a separately approved migration map.
- Focused isolated target MySQL `PosShellTest::test_admin_photo_is_private_self_scoped_mime_limited_and_audited` **1/1 PASS (28 assertions)**: malformed image and subject injection 422, authenticated owner upload/read, other Admin isolated, owner delete and retained identity audit. Frontend typecheck and production Vite build PASS. Actual Edge account upload/render/persisted reload, anonymous and different Admin denial, removal and subsequent 404 **1/1 PASS (25.6s)**. Synthetic browser fixtures cleaned; initial project-owned MySQL stopped state restored after each run. No real profile files, owner identity or original source media changed.
- Previous browser selector and concurrent session failure signatures were not retried; this was a **different new feature test**, passing on the first attempt. GitHub Actions billing pre-run blocker remains; exact-candidate clean-checkout CI is not verified. P01 remains Pending for read-only scoped POS audit viewer, saved portal list/search preferences, owner unassigned-outlet editing/historical archive and authorized private real-data cutover. MT-7.5 In Progress, MT-7.6 unstarted.
- New PHOTO plus five existing P01 browser cases joined with distinct synthetic photo-user fixture (separate from password-change fixture): **real Edge 6/6 PASS (1.9m)**. New PHOTO plus six existing backend tests joined: **7/7 PASS (201 assertions)**. POS E2E cleanup additionally deletes only UUID-named account-private synthetic photos after its DB transaction; every run restored the initially stopped target MySQL. No equivalent failed-selector, session, or billing CI retry was performed.

## P01 protected POS audit viewer (19-Sep-2026; bounded local acceptance)

- Original source SuperAdmin audit route 39 maps to protected Full Access **single Admin** `GET /internal/admin/pos/audit`; requires both existing owner-level permissions and an active protected role, not merely a hidden link. Reads the existing `pos_audit_logs` (including archived outlets) with search, actor type, method, UUID-outlet filter and 60/page. No mutations or alternate credential realm; original source audit history import is a separately authorized HOLD.
- Publicly rendered event DTO includes time, actor name/email, outlet label, action, method, path and status **only**. Historical raw `payload`, IP and user-agent columns are never projected. Sensitive viewer response uses `private, no-store` and `no-referrer`; direct guest and non-protected Admin requests are denied.
- Isolated target-only MySQL `PosShellTest` **8/8 PASS (277 assertions)** including new audit permissions, filter, invalid-input, read-only and 63-row two-page cases; frontend TypeScript/Vite production build PASS. Focused real Edge audit **1/1 PASS**, then materially expanded profile-edit+audit **2/2 PASS**, then actual joined P01 POS browser **7/7 PASS (2.3m)**. Synthetic fixtures cleaned; owned MySQL returned to its initially stopped state after every run.
- **LOOP_GUARD diagnosis:** initial focused browser 422 after outlet filter: `public_id as id` was cast by Eloquent as an integer primary key, so UI submitted `outlet=91` rather than UUID. Replaced with explicit public-ID DTO; focused test PASS. First joined suite's new forced sign-out of prior owner left navigation pending despite HTTP 200 and caused same-owner device-cap login 403 in the audit case; removed added logout wait, assigned a **distinct synthetic protected audit owner**, and limited cleanup to named test accounts. Second joined run's static `E2E Sales Outlet` option label became stale because an earlier P01 profile test renamed E41; replaced label selector with observed immutable E41 code and UUID value, verified changed-name two-case **2/2 PASS** before final joined **7/7 PASS**. No unchanged failing scenario repeated.
- P01 **remains Pending**: portal presentation/list-search preferences, owner unassigned-outlet editing/historical archive policy, authorized real source identity/photo/audit cutover, actual recovery delivery/owner signoff and GitHub Actions billing-blocked exact-candidate CI are not accepted. MT-7.5 In Progress; MT-7.6 not started.

## P01 portal preference source rows 40/41 (19-Sep-2026; partial local acceptance)

- Reconciled all nine source registry defaults/options and the SuperAdmin preferences GET/PUT to a protected, audited unified Admin `GET/PUT /internal/admin/pos/portal-preferences` with a real Inertia/React form. Only active protected Full Access holders with `config.portal-presentation.manage` can read/save the global `portal.*` entries. Exact allowlist and source enum/boolean types; atomic `pos_settings` persistence and identity audit, no new realm, provider or source mutation. `identity.recent` and the unchanged identity write throttle protect PUT.
- New target-only `PosShellTest` joined **9/9 PASS (323 assertions)**, including guest/operator denial, default values, injected-key/invalid-option denial and persisted audit. Real Edge focused owner form **1/1 PASS** and expanded joined P01 **8/8 PASS (2.3m)** with actual saved values reloaded. Synthetic preference-owner fixture was isolated from prior owner sessions; e2e setup requires an empty portal test baseline and teardown removes only test-owned nine settings when the fixture's audit marker is present on `mobisttech_test`. Original project-owned MySQL stopped state restored.
- LOOP_GUARD: new backend test first failed 403 because the Full Access fixture lacked an assigned active outlet; fixed actual assignment without relaxing login policy. Second failed 429 because guest/operator/three invalid/success attempts exceeded the independently verified identity 5-per-minute path+IP throttle; reduced duplicate HTTP invalid attempts and separately checked enum contract, preserving rate limiting. Materially corrected joined backend PASS; the new focused and joined Edge runs PASS first attempt. GitHub billing-blocked CI was not dispatched.
- **Not closed:** saving defaults is not evidence that legacy invoice/inventory/warranty/claims list screens consume them. Current target customer-reporting lists are capped at 100 rows and do not yet implement the original server-side per-page/search-category/focus/remember behaviors. Next concrete P01 action is to wire authoritative portal defaults into an actual outlet-scoped invoice/claim list query and UI with greater-than-100 fixture coverage, then reconcile inventory/warranty. P01 Pending; original data cutover, owner archive scope, recovery delivery and exact-candidate remote CI remain separate HOLDs. MT-7.5 In Progress, MT-7.6 unstarted.

## Current CI gate correction (19-Sep-2026; supersedes older billing-blocked statements)
The repository is now PUBLIC (verified with GitHub API). Run 35442617990 remains a historical pre-publication failed runner-start record, not a confirmed current billing blocker. MT-7.5 parity work continues locally. At the actual required CI milestone, verify the exact candidate and run ONE fresh manual public-repository CI if no equivalent success exists. Only a fresh post-public billing failure may be reported as a current blocker. Do not dispatch CI solely to prove billing state or repeat historical billing warnings in routine Incomplete output.


## P01 invoice/claims preference consumers (19-Sep-2026; local bounded acceptance)

- Actual outlet-scoped `/internal/admin/pos/customer-reporting/{invoices,claims}` now applies server-side validated page, typed search category and query against the source-approved global `portal.*` defaults/values, rather than silently truncating at 100. MySQL paginates with exact 10/15/25/50/100 configuration, scoped joins, explicit row projection and 422 for invalid categories/pages or injected outlet filter. Invoice customer history remains tied to the returned invoice page; claim intake sale candidates remain a separate bounded list, not falsely counted as paginated history. The real React workspaces show search/category/previous/next and configured page size; invoice selection follows the visible page. Search autofocus respects its saved boolean.
- Target-only MySQL `PosCustomerReportingInterfaceTest` **4/4 PASS (135 assertions)**: 126 local invoices and 125 claims, ninth-page access beyond the old 100-row cap, saved page sizes 50/25 and default categories, typed search, invalid query rejection and an extra second-outlet invoice excluded from counts/search. Previous `PosShellTest` **9/9 PASS (323 assertions)**; TypeScript/Vite production build PASS. Focused real Edge history browser **1/1 PASS** with 122 synthetic invoices and actual search/page navigation; new history test + previous P01 POS browser joined **9/9 PASS (2.6m)**. After final claim-detail/document-selection reset fix, rebuilt TS/Vite PASS, focused real Edge history 1/1 PASS and existing MT43 document-UI regression 1/1 PASS. All browser fixtures and original stopped test MySQL state restored.
- **Partial P01 only:** inventory and warranty receipt list preference consumption, opt-in remember-search lifetime/privacy decision, historical-used outlet archival/unassigned owner policy, authorized original data migration and owner recovery delivery remain unclosed. A fresh exact-candidate CI run remains a separate required acceptance gate; the old pre-publication CI failure must not be presented as a verified current billing blocker. No equivalent failed test was rerun without a material change; LOOP_GUARD remains enforced. MT-7.5 In Progress; MT-7.6 unstarted.

## P01 inventory and warranty-receipt saved-list consumers (19-Sep-2026; local bounded acceptance)

- Original nine global portal preferences now reach actual **inventory** and **warranty-receipt** lists as well as prior invoice/claims history. Inventory uses separate authorized `mode=inventory` on the existing outlet-scoped catalogue, configured 10/15/25/50/100 MySQL page size, strict category allowlist and matching product/SKU/brand/model/variant/unit/IMEI/stock fields; Sales retains its original 20-item catalogue and scan/lookup behavior. Warranty receipts reuse the protected outlet-constrained claim/invoice/product query but read independent `portal.warranty_search_category`, configured claims page size (source has no separate warranty page-length setting), customer/CNIC/invoice/product/IMEI search, and direct-denial policy. UI category selectors, pagination and configured search autofocus are rendered; stale visible invoice/claim selection is cleared on page changes. Search filtering and `mode` cannot override server-selected outlet.
- Isolated MySQL `PosTransactionInterfaceTest` + `PosCustomerReportingInterfaceTest`: **7/7 PASS (227 assertions)** using 112 inventory products, 125 warranty claims, independent saved values, 100+ page access, category searches, invalid-input and cross-outlet negative checks; sales catalogue 20-item contract unchanged. TS typecheck/Vite production build PASS. Real Edge inventory+warranty UI focused **1/1 PASS** on 32 inventory products and 22 warranty receipts with page navigation, category search, assigned E41 outlet and search-field autofocus; synthetic test-only fixture/teardown guarded by `mobisttech_test`, with owned MySQL returned to stopped baseline. A real joined-browser regression is tracked separately and may only be counted PASS upon terminal evidence.
- **Still P01 Pending:** original warranty-intake invoice/sale selection (distinct from warranty receipt history) is not closed by this receipt-list result; saved `remember_search` browser retention/privacy and lifetime are not implemented; original identity/photo/audit data cutover, historic outlet archive/unassigned-owner authorization and recovery delivery remain separate owner/external HOLDs. An exact-candidate GitHub CI remains unverified, not presumed billing-blocked from the historical pre-public run. LOOP_GUARD: initial backend test's warranty default incorrectly used hard-coded claims area; explicit area propagation fixed and 7/7 PASS. Separate file-edit command quoting failures used a distinct Python script, not an unchanged failing test rerun. MT-7.5 In Progress; MT-7.6 unstarted.

### P01 joined regression and LOOP_GUARD closure (19-Sep-2026)

- First expanded Edge run **8/11 PASS, 3 FAIL** due test-fixture interference, not inventory/warranty backend failures: the new inventory test held the same `e2e-inventory` login used by later POS shell/mobile and photo-denial browser; the later transaction browser tried the shared `e2e-sales` password after the prior account test had intentionally changed it. Read-only browser error-context and existing session/password test scripts established these distinct source/harness signatures; neither login policy nor application password/device limit was weakened. Same-class retry was stopped pending the complete suite identity audit.
- Material strategy fix: provision **three separate, target-test-only account fixtures** for inventory browser, warranty browser and transaction browser, each with the existing authorized role/outlet; add all to bounded E2E cleanup. Then actual full joined Edge customer-reporting + inventory/warranty + P01 POS shell + sales transaction **11/11 PASS (3.4m)**; prior account/photo/role tests still pass unchanged. After additional explicit sales-only inventory-mode direct-denial assertion, joined local `PosShellTest` + `PosTransactionInterfaceTest` + `PosCustomerReportingInterfaceTest` **16/16 PASS (551 assertions)**. Target-only MySQL restored to its initial stopped state; no original-source/business data, provider or production changes.
- P01 is **not** marked Complete: warranty-intake selection and opt-in remember-search browser retention/privacy are still unclosed, and owner/external migration, recovery, archival and fresh exact-candidate CI remain separate gates. MT-7.5 remains In Progress; MT-7.6 not started.

## MT-7.5 fast-track review authority (19-Sep-2026; latest status pointer)

The **single consolidated current gap classification and finite 27-task checklist** is `docs/audit/MT-7.5_FAST_TRACK_REVIEW.md` (relative file: `MT-7.5_FAST_TRACK_REVIEW.md`). It supersedes earlier *next-action* sentences in this chronological file without rewriting accepted historical test evidence. Baseline: 22 original feature families + five cross-gates, 5 DONE / 22 OPEN = **18.52% gate closure**; roadmap remains 53/56 points, MT-7.5 In Progress, MT-7.6 not started. The existing P01 warranty-intake latest-100 gap is the next ready substep; no new implementation or test run belongs to this review-only checkpoint. Exact-candidate CI is unverified; the old pre-public CI billing failure cannot be presumed current.

### D05 synthetic offline owner recovery browser acceptance (20-Sep-2026)

The already-implemented default-OFF, unbound offline-owner recovery target passed enabled-code actual Edge 1/1 on a uniquely scoped synthetic owner account: owner-only enrolment panel, eight one-time codes, hide/reload non-retention, guest recovery and revoked prior session. Default-OFF legacy recovery Edge 1/1 PASS; backend 3/3 PASS (79 assertions); initial test MySQL stopped state restored and guarded synthetic code/account cleanup PASS. No real owner-account enrolment or real recovery code issuance; Gmail OFF. D05 implementation locally accepted only; P01 stays Pending for D03 historical archive, D04 isolated source-copy rehearsal, remaining source parity/identity acceptance and separately approved owner rollout. 6/27 DONE / 21 OPEN, MT-7.6 UNSTARTED.

## 20-Sep-2026 fresh-business operative closure rules

This section supersedes all earlier CURRENT-NEXT/HOLD claims here that require old-source business records, real-source migration rehearsal, old POS/Website identity mapping, legacy snapshot acquisition, old financial settlement, old invoice reconstruction or source-data cutover. Preserve historical test evidence and complete source-feature/route/business-rule disposition as read-only reference. D04 is RETIRED/NOT APPLICABLE and cannot block P01/Q01/MT-7.5. P01 D03 future-history archival is separate target-only behavior: keep fail-closed guards and NEW recorded business-history integrity but never gate new-outlet operation or independent feature families on source records. No previously completed family is reopened or counted twice.

P01 next bounded acceptance is a joined secure fresh-target Admin login -> new outlet create/configure -> inventory/customer -> POS sale/payment/return/warranty/reporting journey in real UI/backend using synthetic isolated target MySQL, reusing accepted unchanged component tests. Advance next independent OPEN family on a demonstrated implementation/evidence gap, not an arbitrary archived legacy-finance counter. P03/P04/P05/P06/W01-W04 close on complete new-target journeys and negative role/money/stock/history security evidence; actual old record import is out of scope. Gmail/payment integration backend, config UI and local/mock acceptance can close independently; authentic OAuth/email delivery and gateway sandbox/live money/refund/bank settlement remain separately EXTERNAL UNVERIFIED (D06/D07 mock-only) pending credentials and approval. Q01 GitHub CI is milestone/final only after authorized runner readiness; independent final owner/legal approvals and MT-7.6 dependency remain. Operative scope: `MT-7.5_FRESH_BUSINESS_SCOPE.md`.

## Current project-wide scheduling overlay (24-Sep-2026)

`docs/PROJECT_EXECUTION_EFFICIENCY_POLICY.md` governs EVERY stage and family. For MT-7.5 the operative finite checklist is the **27 stable IDs** in `docs/audit/MT-7.5_FAST_TRACK_REVIEW.md`, with actual latest completion/next-action evidence in `docs/PROJECT_IMPLEMENTATION_STATUS.md`; historical rows in this gate index are not automatically current. Do not append speculative W04 subgates, reopen accepted families or repeat source crosswalks without documented regression. Close W04's documented non-HOLD functional/negative/browser gates once supported by adequate evidence, keeping genuine H-02 separate and Q01 final cross-system acceptance intact. This overlay changes scheduling only, not existing security/provider/owner gates or any family state.

## W05 development closure and Q01 fixture-baseline boundary (24-Sep-2026)

`W05` Complete (development-only), evidence `MT-7.5_W05_DEVELOPMENT_CLOSURE_2026-09-24.md`: source 24 files/nine routes disposed, guarded real Edge browser 7/7, isolated PHP service/digital 10/10 (103 assertions), confirmed signed-in unrelated Customer denial and authorized real Admin/Customer lifecycle including expiry/paid-only completion and private history in all three modes. This family closure does not mark Q01, H-02, MT-7.5 or launch complete. Separate Q01 clean-candidate schema gate remains OPEN: 3 rate-limit buckets of unverified pre-run ownership detected on shared disposable test schema and explicitly NOT deleted; establish independently owned clean baseline or prove exact fixture ownership before any final zero-residue assertion. Next family `W06`, 17/27 development gates DONE / 10 OPEN. Earlier static inventory counts at the top reflect their original historical baseline, not current status.
