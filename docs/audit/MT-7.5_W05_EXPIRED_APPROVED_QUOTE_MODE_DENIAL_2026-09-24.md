# MT-7.5 / W05 expired approved quote owner-HTTP boundary (24 September 2026, LOCAL)

Existing stable `18/W05` gate remains OPEN [E]; MT-7.5 development index 16/27 DONE, 11 OPEN. Source parent `bebf345ec954eb4486945081e31a70e5470c3f9c`. W04 development remains DONE and H-02 authentic payment remains separate PRE-LAUNCH PENDING, external methods OFF.

- Demonstrated residual: the existing service test denied approval of an already expired *draft* but did not exercise a previously **approved** quote expiring before a newly initiated Customer milestone through the real Customer HTTP boundary with a subsequent Website-mode switch. Added one rollback-only `ClientProjectServicesTest` case: approve an exact PKR 1000.00 synthetic proposal, register a synthetic available gateway to isolate the expiry guard (not a real merchant), advance the test clock 15 days, freshly authenticate its owner and GET the historical project (HTTP 200, unchanged amount, `payable=false`). Publish `commerce_only` using the existing synthetic Admin and verify the owner still reads the proposal with `payable=false`; attempted fresh milestone POST returns 409 and order/payment/idempotency counts remain unchanged. Original approved quote status is retained. Clock restored in `finally`; no production account/provider, stored external credentials or source data touched.
- Exclusive LOCAL isolated `mobisttech_test@127.0.0.1:13306` test wrapper: focused PHPUnit **1/1 PASS (10 assertions)**; affected W05 PHP service/owner/expiry/paid-neighbor family **7/7 PASS (78 assertions)**. Scoped Pint initially reported a file style issue; material scoped formatter correction was run, after which `pint --test` **1/1 file PASS** and diff whitespace passed. The sole tracked change is the new test method; no application code or other historical test assertions changed. These backend assertions do NOT constitute authentic gateway, full real-browser expiry, or exact-source Q01 acceptance.
- First pending W05 finite non-HOLD action: reconcile genuinely unverified signed-in foreign Customer actual browser/project-file/milestone mode negatives against existing rollback-only HTTP owner tests, then perform a bounded joined W05 family closure when the actual checklist is satisfied. Existing 24/9 source mapping, checkout W04 and already-accepted status/browser cases must not be rerun without regression.
