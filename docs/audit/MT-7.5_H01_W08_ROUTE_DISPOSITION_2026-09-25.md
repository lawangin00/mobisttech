# H01 bounded source-route reuse: W08 integration health (25-Sep-2026 LOCAL)

This is a READ-ONLY H01 disposition of the **two original W08 Website routes**, not a reopening of 02/W08 (already DONE), a new product test, authenticated Gmail/Drive connectivity, or full H01 closure. Frozen `docs/migration/SOURCE_SYMBOL_INVENTORY.json` is the source identity authority. The old Website Laravel integration test endpoint is not a generally executable arbitrary URL/command/probe contract; it is retired in favor of the approved single Laravel backend's provider-allowlisted status/test service.

| Original exact route and source action | Approved independent target route/role/behavior | Prior target evidence and boundary |
|---|---|---|
| `GET|HEAD admin/integrations` — `WebsiteIntegrationStatusController@index` | Protected `GET /internal/admin/settings/integrations` (`IntegrationController::page`, Inertia `integrations.tsx`) and JSON `GET /internal/admin/integrations` (`IntegrationController::index`), both using `IntegrationManager::statuses` with Admin integration authorization; masked status only. Separate source Website Admin guard and Blade page retired. | Existing `UnifiedAdminGoogleIntegrationTest.php`, `docs/operations/README.md`, and 02/W08 stable DONE readiness; no provider tokens returned to UI. |
| `POST admin/integrations/check` — `WebsiteIntegrationStatusController@check` | Protected `POST /internal/admin/integrations/{provider}/test` (`IntegrationController::test` -> `IntegrationManager::test`) restricts provider to approved `gmail`/`google_drive`, authorization and `services.google.enabled` fail-closed; front end exposes Test on an authorized connected provider. Old source generic `check` URL retired, not publicly aliased. | Reuse existing `UnifiedAdminGoogleIntegrationTest.php` and `docs/deploy/MT-7.4_VERIFICATION.md`; external tests require official authorized connection and are NOT claimed here. |

Exact source method/URI/action validated against two immutable `W08` source-route entries. Source local files, source service, provider API and test DB never executed. W08 remains DONE; H01 remains OPEN because other source route dispositions and open P01/Q01 family boundaries remain to reconcile.
