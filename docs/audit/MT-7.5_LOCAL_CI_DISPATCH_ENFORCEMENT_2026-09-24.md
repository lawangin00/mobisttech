# MT-7.5 — LOCAL GitHub CI dispatch enforcement (2026-09-24)

## Incident and scope

- In the LOCAL execution mode, source `027d7364875b95f25f77192c735e4bc7de35b8f1` already had local Website browser 18/18 PASS and typecheck PASS. GitHub CI #103 (`35998598423`) subsequently ran full clean-checkout acceptance from `workflow_dispatch`; the existing control gate unconditionally admitted manual events. This is an uncontrolled *dispatch route*, not proof that the project owner initiated or approved that run.
- Universal authority: `lawangin00/references/UNIVERSAL_CI_REQUEST_DISPATCH_POLICY.md` v1.1 and `UNIVERSAL_EXECUTION_MODE_POLICY.md` v1.6. This project inherits them via `docs/AI_PROJECT_COMMAND_REGISTRY.md`. In LOCAL and DIRECT_LOCAL, ordinary tests run on the PC, with hosted verification reserved for a genuine recorded milestone/release/completion or distinct necessary independent acceptance. Merely changing the source SHA or repeating an already-PASS local test is insufficient.

## Implemented controls

1. `tools/ci-mode-gate.py` refuses `workflow_dispatch`, rejects LOCAL/LOCAL_WORK routine stage requests, validates project identity, exact parent/source SHA, approved gate/focus, execution surface, and requires a nonempty authorization reference, prior local-test evidence and specific hosted-only need before allowing a LOCAL milestone/necessary request. The controller must check these references against the live ledger BEFORE writing the request commit; the CI gate checks their presence/structure, not the truth of arbitrary prose.
2. `.github/workflows/ci.yml` and `.github/workflows/github-only-website.yml` no longer advertise an unrestricted manual event; only an exact `.github/ci-requests/**` push can start them, followed by the gate's self-test and real mode/request validation BEFORE the expensive acceptance job.
3. No CI-request commit is generated by this reconciliation. The existing MT-7.5 / W04 work, HOLDs, previous run receipts, historical roadmap and local acceptance state are not advanced or rewritten.

## Regression and operational acceptance

- Run `py -3 tools/ci-mode-gate.py --self-test` after safely syncing the exact current remote; it tests LOCAL routine rejection, unapproved manual rejection in LOCAL/GITHUB, justified LOCAL milestone and necessary cases, LOCAL_WORK behavior, project mismatch and mode exceptions.
- Verify both active routine workflows expose only `push` filtered to `.github/ci-requests/**`. Ordinary code/doc pushes must generate **zero** CI runs. Do not dispatch a real costly workflow merely to test a forbidden path; test the pure gate locally.
- Before any future hosted verification, check current universal mode, the exact project ID, milestone/necessary authorization, ledger evidence of locally executed checks and a distinct hosted-only need. Commit only a source-bound, approved request and record exact run/terminal outcome. An absent or inconsistent authorization/evidence leaves hosted verification PENDING, not auto-dispatched.
- The separate historical `deployment-rehearsal.yml` is a manual MT-7.4-specific release rehearsal rather than a routine development CI workflow; it is not used as a replacement for the blocked generic `workflow_dispatch` route. Its own use still requires independently verified release/necessity authorization before invocation.

## Verification scope

GitHub content writes and re-reads establish the remotely persisted controls. Local gate execution and safe local fast-forward remain pending until the authorized Windows clone is synchronized; do not label local regression PASS or claim that GitHub Actions ran as part of this fix.
