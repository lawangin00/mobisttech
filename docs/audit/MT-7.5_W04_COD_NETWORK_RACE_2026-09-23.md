# MT-7.5 W04 — two-server authenticated network COD race (23-Sep-2026)

State: Bounded target-only real localhost HTTP transport PASS; W04 IN PROGRESS, 15/27 DONE, 12 OPEN. No genuine merchant, external provider, bank settlement or production acceptance.

- `backend/tests/Feature/W04CodNetworkRaceTest.php` starts two independent project-owned PHP web servers on 127.0.0.1 ports 18171/18172; port preflight refuses occupied ports. Each serves the existing Laravel app against isolated `mobisttech_test` with database sessions. The test creates two synthetic permissioned Admins/outlets and obtains distinct login, outlet-selection, session-cookie and CSRF credentials through genuine protected HTTP-kernel routes.
- `backend/tests/Support/W04CodNetworkWorker.php` issues separate localhost socket HTTP POST requests through PHP cURL to each web server, with its own authenticated session and CSRF header. Both processes reach a readiness barrier while the parent holds the unchanged production COD-scoped MySQL named lock.
- Two concurrent drafts return 201/201 with distinct versions 1 and 2; two concurrent attempts to publish the same latest draft return 200/409, yielding exactly one published COD revision. Actual HTTP transport, Admin authentication, authorization and CSRF middleware remain active. No external provider or merchant credentials are activated.
- Isolated network test 1/1 PASS, 31 assertions. Scoped Pint corrected only new test/harness style; joined network + previous COD protected HTTP, two-worker and HTTP-kernel tests 6/6 PASS, 156 assertions. PHP syntax of both new files passed. Tests refuse an existing COD revision baseline and remove only identified synthetic revisions/audit/session/Admin/outlet records and project-owned test servers; the isolated MySQL instance is restored to its initial stopped state.
- Evidence boundary: localhost two-server network contention is not production traffic or an authentic gateway acceptance test. Remaining W04 payment/mode/refund/merchant authority and external sandbox/live tests remain pending or H-02 HOLD as applicable.
