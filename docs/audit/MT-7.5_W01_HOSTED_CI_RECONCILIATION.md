# MT-7.5 W01 hosted CI reconciliation

Status: IN PROGRESS. W01 remains open (13/27 complete, 14 open); P07 remains complete. Hosted W01 full-gate retry is under LOOP_GUARD after three unsuccessful/incomplete attempts; no terminal full-gate PASS exists.

- Source candidate: `27520b504026792d57eb823c937f6d2fabb3f08c`; exact-source request commit: `a5e3022ddbbfd81ae3e54916aa261d29bbd1fd0d`.
- Hosted attempt 1, full CI run `35591937159`, acceptance job `106308096867`, terminal FAILURE: full backend regression 337 passed / 1 failed (10,000 assertions). Later browser gates, including W01 Customer browser, were skipped, not passed. Exact failure: `PosCustomerReportingInterfaceTest::test_invoice_customer_documents_delivery_retry_and_report_export_http_contract`, line 106. WhatsApp invoice document HTTP returned 500 instead of 200 because `PosCustomerReportingController::whatsapp` passed raw binary PDF in the JSON response; Laravel rejected malformed UTF-8. This is a POS document JSON-transport regression exposed by independent W01 hosted verification, not proof of W01 Customer failure. Material fix at `7c7719b17dc42d8dbd32ff60c97e62bbf2e24a34`: normalize only the WhatsApp HTTP attachment's `pdf` bytes to JSON-safe `pdf_base64` and remove the raw binary key, mirroring the existing document PDF endpoint. Underlying PDF bytes/hash, durable delivery attempt, send permission, customer/outlet scope and WhatsApp URI logic are unchanged. The existing positive WhatsApp HTTP test catches the previously terminal HTTP 500.
- Hosted attempt 2, distinct retry run `35592726162`, request `c1370436aab9eab7e5fe4f2f3aa39b3ebfa36808`, terminal FAILURE in CI mode preflight before any backend/browser execution: unauthenticated GitHub API returned HTTP 403, `rate limit exceeded`, reading canonical `UNIVERSAL_EXECUTION_MODE.json`. This is an infrastructure/state-read error distinct from the prior PDF binary regression; it does not establish product regression. Material CI gate correction `9755f8cb5cec1d9b64f27a081372b07a40e80a49`: retain canonical API as primary and, only on HTTP 403, read the same public canonical main-branch mode JSON through raw.githubusercontent.com using a per-run cache-busting query; fail closed if both read paths fail or state schema/mode is invalid. Request/source parent guard is unchanged.
- Hosted attempt 3, run `35592971634` at request commit `e333aa3a21be09897a5d43656de1b772bdf34088`, acceptance job `106311327099`, terminal FAILURE (21-Sep-2026). Canonical mode preflight and backend PHP regression PASS (338 tests / 10,016 assertions); frontend typecheck, lint and production build also passed. POS/Admin Playwright gate was 15 passed, 17 failed, 1 skipped; later Website Customer browser gates were skipped and cannot be claimed accepted. Failure signatures include legacy fresh-stock browser asserting `Qty 0` inside a product button now containing only product name/code; repeated synthetic variant option seeder refusal `Synthetic variant option must not overwrite existing options`; a linked-history fixture expecting an empty accessory baseline; owner-login/navigation timeouts and an HTTP 422 login, with possible prior failed-test state contamination. Do not attribute all 17 to one established root cause or to W01 Customer behavior: this run never reached that gate.
- LOOP_GUARD / materially changed next strategy: Do NOT dispatch an equivalent full CI rerun or reuse a dirty sequential fixture set. First inspect the precise first-failure trace and fixture setup/teardown ordering; separately verify the actual current product quantity locator versus stale legacy assertions. Run a deliberately isolated, clean-schema W01 Customer signed-order/profile/avatar and last-owner/cross-realm browser journey with its own fixture and terminal result, then independently repair/reconcile the separate POS/Admin suite fixture interactions and rerun only the affected focused browser groups. Only after a demonstrated material correction and isolated terminal evidence should an exact-source full hosted gate be dispatched; inspect the terminal result before closing W01. No protected legacy repository, live customer/provider/production data or other family is authorized by this audit.
- W01 source-only fixture diagnosis (21-Sep-2026 PKT, no new test run): `backend/tests/browser/global-setup.ts` seeds the shared POS baseline once; `global-teardown.ts` cleans it only at the end. `pos-master-data-imported.spec.ts` directly invokes `PosMasterDataVariantE2eSeeder` before its imported seeder, and the separate `pos-master-data-variants.spec.ts` invokes that SAME variant seeder in the same serial Playwright database. The variant seeder intentionally refuses any pre-existing (`list_key`, `code`) and outlet `908`; its changes are committed as browser fixtures rather than isolated per spec. Therefore, the second invocation in a shared unchanged schema necessarily collides even if the first spec's product behavior passes. The separate first-failure `mt75-fresh-next-smoke.spec.ts` asserts `Qty 0` within a product-name button, while the previous CI trace shows the button currently lacks that quantity text; verify the actual current stock projection separately before changing its selector. `tests/browser-website/customer.spec.ts` already contains the signed historical-order/profile-photo browser test, but the hosted run skipped the entire Customer gate. Focused next execution must use a fresh disposable schema and a separate W01 Customer gate; do not merely re-run all 33 POS tests or relax the variant seeder's fail-closed uniqueness guard. After independent terminal Customer evidence, repair the POS/Admin test fixture lifecycle via explicitly scoped cleanup or per-spec isolation, and prove each changed group before a whole-gate attempt. No new local or hosted tests, application-code mutation, production action or family closure are claimed for this read-only diagnosis.
- W01 isolated Customer browser execution (21-Sep-2026 PKT): an allowlisted `MT-7.5` / `W01-customer` exact-source CI request scope was added to `tools/ci-mode-gate.py` (`af7c8d854c4c6d931c5586512e2f9514f155fcb5`) and `.github/workflows/ci.yml` (`565e1feaf2aa6213ba29ed05edc760c8904e2b2b`) without changing the full CI path; the request-only child commit `0189e0e7f844eabcc6e3157c54ee6438cbd59ee0` passed the canonical mode/parent guard and started run `35598411944`. Independent website clean checkout run `35598412051` passed. In `35598411944`, fresh disposable MySQL migration, PHP Pint 268 files, backend typecheck/production build (589 modules), website typecheck/lint/build, public budgets and secret scan PASS. Crucially, the isolated real Chromium W01 Customer browser scenario in `tests/browser-website/customer.spec.ts:151` **1/1 PASS (12.1s)**: owned signed 30-minute historical-order summary and Customer profile name/mobile/photo upload, removal and restoration. Global synthetic fixture cleanup and cache clear PASS. The full backend, POS/Admin and all other browser groups were intentionally SKIPPED for this focused request; last-owner and cross-realm browser gates remain OPEN. **Overall isolated CI run terminal FAILURE** because the subsequent independent full API schema gate reported `expected 170, actual 171` tables. Source diagnosis: migration `2026_09_20_130000_add_offline_owner_recovery_codes.php` creates retained `owner_offline_recovery_codes`, but the `api` expected-table construction in `tools/migration/verify_mysql_schema.php` does not include that new table. Do not equate the browser PASS to overall CI PASS, suppress the schema failure or change the expected count without verifying exact table identity and column/index contract. Material next: update the full schema verifier's current API expected set to include precisely the owner recovery table and verify its migration-defined columns/index; then independently fix the POS/Admin shared fixture lifecycle and obsolete stock quantity locator with targeted evidence, before attempting any full CI run. W01 remains OPEN, 13/27 DONE, 14 OPEN; no production/provider/protected-source action.
