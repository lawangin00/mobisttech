# H01 W03 exact original cart/order/review route disposition (25-Sep-2026 LOCAL)

Frozen W03 original **16 route methods/URIs** are linked below to already accepted `docs/audit/MT-7.5_W03_CART_ORDER_REVIEW_PARITY.md` grouped decision, `docs/audit/MT-7.5_X01_RESERVATION_PARITY.md` and current unified Admin/Customer/Next routes. Four Admin order routes overlap the existing exact X01 source crosswalk. No accepted family behavior is reimplemented or retested here; old public session-cart/anonymous order-number security is intentionally retired, not aliased.

| # | Frozen original method/URI | Approved existing target behavior / retirement | Prior evidence and limits |
|---:|---|---|---|
| 01 | `GET\|HEAD admin/orders` | Protected `GET /internal/admin/website-commerce` and `/internal/admin/website-commerce/orders`; one unified Admin permission, no public old Admin route. | Prior accepted W03 Admin list, role and X01 joined order-state evidence. |
| 02 | `GET\|HEAD admin/orders/report.csv` | Protected `GET /internal/admin/website-commerce/orders.csv`, JSON-safe export and authenticated download in React Admin. | W03 hosted protected browser CSV evidence; X01 shared stock/money state. |
| 03 | `GET\|HEAD admin/orders/{order}` | Old standalone server-rendered Admin detail URL intentionally retired; protected `/internal/admin/website-commerce/orders` bounded row/detail projection plus Customer-owned history and canonical document access. | W03 closure explicitly approved retiring duplicate detail route; never expose other Customer private order. |
| 04 | `PATCH admin/orders/{order}` | Protected `PATCH /internal/admin/website-commerce/orders/{order}` -> `WebsiteCommerceAdministration::updateOrder`; no old public Admin guard. | W03 Admin forward-state and X01 joined order-stock contract, role denial reused. |
| 05 | `GET\|HEAD admin/reviews` | Protected `GET /internal/admin/website-commerce/reviews` and React commerce moderation UI. | W03 purchase-qualified list, Admin review permissions and actual browser evidence. |
| 06 | `PATCH admin/reviews/{review}` | Protected `PATCH /internal/admin/website-commerce/reviews/{review}` -> `WebsiteCommerceAdministration::moderateReview`. | Prior W03 immutable moderation audit/role test and hosted browser evidence. |
| 07 | `GET\|HEAD cart` | Next `/cart` owns on-device cart, quote/reprice via authenticated `POST /api/customer/cart/quote`; old server session-cart write endpoint retired. | Existing W03 customer/cart/quote and X01 server-authoritative stock acceptance. |
| 08 | `POST cart` | Next `/cart` owns on-device cart, quote/reprice via authenticated `POST /api/customer/cart/quote`; old server session-cart write endpoint retired. | Existing W03 customer/cart/quote and X01 server-authoritative stock acceptance. |
| 09 | `PATCH cart/{lineKey}` | Next `/cart` quantity/remove client state, then server-authoritative quote; old server-lineKey session mutation URL retired. | W03 source cart state intentionally adapted rather than preserving old server session. |
| 10 | `DELETE cart/{lineKey}` | Next `/cart` quantity/remove client state, then server-authoritative quote; old server-lineKey session mutation URL retired. | W03 source cart state intentionally adapted rather than preserving old server session. |
| 11 | `GET\|HEAD checkout` | Next `/checkout` public page with authenticated Customer `POST /api/customer/orders` via same-origin proxy; no anonymous old source POST checkout mutation. | W03 actual customer checkout and X01 idempotent stock/payment/reservation join. |
| 12 | `POST checkout` | Next `/checkout` public page with authenticated Customer `POST /api/customer/orders` via same-origin proxy; no anonymous old source POST checkout mutation. | W03 actual customer checkout and X01 idempotent stock/payment/reservation join. |
| 13 | `POST customer/reviews` | Customer-only `POST /api/customer/reviews` -> `CustomerApiController::submitReview` and Next account/review controls. | W03 purchase qualification, owner/role denial and Admin moderation accepted. |
| 14 | `GET\|HEAD order/{orderNumber}` | Authenticated Next `/account/orders/{order}` with owned `GET /api/customer/orders/{order}` and signed minimized historical-order capability; old public enumerable orderNumber detail retired. | W01 signed history and W03/X01 owner-private order behavior reused. |
| 15 | `GET\|HEAD order/{orderNumber}/invoice` | Owner-scoped order history plus canonical document services; original public orderNumber invoice endpoint retired to avoid anonymous invoice exposure. | W03 closure canonical document retirement and P06 protected document privacy accepted; do not assert anonymous invoice link. |
| 16 | `GET\|HEAD order/{orderNumber}/payment` | Authenticated Next `/account/orders/{order}` and owner `GET /api/customer/orders/{order}` show payment status; Customer-owned retry endpoint preserves idempotent payment authority. | W03/W04/X01 owner-scoped payment/replay evidence reused. |

## Scope and acceptance boundary

Existing W03 accepted backend/hosted Admin/Next Customer evidence already covers checkout, cart mutation, export/review moderation, private order status and stock authority; no new test or source execution was necessary to index these exact 16 original routes. Original public invoice/payment/order-number URI and legacy authentication are not silently restored. Any requirement for a separately downloadable authenticated customer invoice must be checked at the separately approved P06/G-C/Q01 document capability gate, not fabricated from source-route count. H01 remains OPEN until the other original route groups and documented pending operator UI/approved retirements are reconciled.
