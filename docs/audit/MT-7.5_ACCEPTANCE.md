# MT-7.5 - Full functional parity and acceptance

Status: In Progress. No final parity, policy approval, production provider or product release claim.

## Scope and evidence boundary

- Canonical project: mobiST Tech (`282dba2f-a2d9-47e8-aa8d-e499fbe1706c`), private `lawangin00/mobisttech/main` only. Protected POS and Website sources remain read-only.
- This point must reconcile `docs/migration/FEATURE_PARITY_REGISTER.md`, addendum v1.1, consolidated requirements and software publishing requirements against actual target backend/UI/public/API tests, security, recovery, CI and operational journeys. Existing individually complete roadmap points are not reopened solely to restate their status.
- Platform foundation/CI and MT-7.4 Linux rehearsal are prerequisite evidence, not proof that every source-to-target feature has full parity. Current register has many still-Pending feature families; they must be accepted individually on fresh evidence, never mass-marked complete based on roadmap point status.

## Initial authenticated Software Product vertical acceptance

- Added `PlatformAdministrationInterfaceTest::test_software_admin_publication_and_rollback_preserve_public_draft_isolation` to join real session+CSRF Admin API and public versioned Software API with one synthetic test-only product. It checks unpublished 404, editor/publisher permission separation, public published revision, private draft isolation, reviewed update, rollback and revision retention.
- This is synthetic test-data acceptance only. It does not publish real mobiST POS, approve reference Privacy/Terms, activate live providers or verify the Next.js public renderer/browser; existing CMS service tests, API contract tests and browser tests are supporting but separate evidence.
- Windows host initially had no disposable MySQL listener on 127.0.0.1:13306. The exact project-owned MySQL helper started this target-only isolated service, the focused corrected test passed 1/1 (20 assertions), then the combined Platform Administration / Website CMS / API contract set passed 15/15 (411 assertions); helper restored the initially stopped database state. The corrected candidate `8349bb24bd5fd4b01157257238bd460cee277ca1` passed full remote clean-checkout CI `35439230226`, job `105887051103` (all backend, isolated MySQL/race, frontend build, Chromium POS/Admin, Website checkout/customer/public, performance/schema and cleanup gates). This accepts the corrected synthetic Software-create slice, not whole-product parity.

## Release decisions and unclosed acceptance

- `docs/reference/mobiST POS-IMS/` is factual reference material, not a production-approved or automatically published Software Product. Its offline local-Windows/database/installer/license descriptions are product-specific; they must be checked against the actual separately delivered mobiST POS product, not substituted with the monorepo's web POS capabilities. Reconcile each Overview, Privacy, Terms and FAQ statement through the reusable Software Product review workflow before public publication.
- Root application license/ownership terms and final legal-policy approvals/effective dates remain explicit owner/legal decisions per `docs/audit/MT-7.2_LEGAL_PRIVACY_AUDIT.md`. Do not fabricate approval or publish placeholder reference text. Provider sandbox/live Gmail or payments and real-data migration retain their independently authorized HOLD boundaries.
- Remaining work: independently complete all parity-register families and cross-requirement traceability, actual end-to-end POS/Website/Control journeys, public Software Product route family and reference reconciliation, fixed Website four-channel/independent POS split/closing/reporting matrix, invoice/warranty document actions, approved legal/footer links and negative security/restore gates. Keep MT-7.5 In Progress until those are evidenced or an explicit unresolved gap is recorded; MT-7.6 is not started.

## MT-7.5 material HTTP defect and corrective acceptance

- Baseline CI at `da3abb553e47b5511175a5edebf77976d2cfbaaf`, manual run `35438503502`, completed successfully; it did not include the new joined Admin/public Software journey.
- First new-journey candidate `25ef56f6cd179e9093ff12deb7f04197e5d7ca94` failed remote clean-checkout run `35438877774` in full backend regression (254 passed, 1 failed); exact test POST `internal/admin/platform/software` returned 404 instead of 200. Other subsequent gates were skipped, so no remote complete acceptance was claimed.
- Local `withoutExceptionHandling()` diagnosis traced the 404 to `WebsiteCms::saveSoftwareDraft()` looking up a non-existent Software public ID at line 410. Controller `softwareDraft(Request, WebsiteCms, ?string $software = null)` consumed the unrelated `identity_realm=admin` route default as its optional positional `software` argument on the create route. This is an actual existing target HTTP route binding defect, not an absent route, CSRF bypass or missing software permission.
- Material fix reads only the named `$request->route('software')` parameter in the controller, treating an omitted Software ID as null; authenticated update retains its explicit Software ID. An isolated published Website-mode test fixture ensures that the initial unpublished Software API 404 is genuinely draft isolation rather than an unpublished site. Two deterministic three-character test outlet codes avoid unrelated fixture-code collisions.
- Focused live isolated MySQL regression PASS 1/1 (20 assertions); three related feature classes PASS 15/15 (411 assertions). No real software product or actual owner-approved policy was published; the synthetic transaction fixtures roll back, and the pre-existing target database stopped state was restored. That exact corrected candidate passed remote clean-checkout CI `35439230226`; remaining source-to-target parity is independently pending.

## Release and requirements-traceability extension

- Expanded the authenticated synthetic Software journey to GET its public Privacy/Terms/FAQ routes, draft a versioned release, prove release invisibility until publish, enforce publisher-only release authority, publish and read release history through the public API. Fresh isolated target MySQL PASS 1/1 (33 assertions); related Platform/CMS/Public API suite PASS 15/15 (424 assertions). Initial target MySQL stopped state restored. This extension is not part of the prior `8349bb2` remote CI result and needs a new exact-candidate clean-checkout PASS.
- `docs/audit/MT-7.5_TRACEABILITY.md` maps each original source family and the four binding requirement sets to concrete target evidence, explicitly listing remaining cross-boundary acceptance. Existing MT-6/MT-7.4 completions are preserved; other families are not mass-closed based on availability of tests. Root application licensing and final owner/legal approvals remain unresolved.

## Protected Software draft-content preview acceptance

- Previously the Software Admin `Preview selected revision snapshot` control exposed only raw JSON. The new protected `SoftwareRoutePreview` presents validated snapshot content through Overview, Privacy, Terms, FAQ and Releases tabs inside the existing authorized Admin page; it neither adds an unauthenticated draft route nor changes publication state. Rendered rich fields come from the existing CMS server-side sanitized snapshots, the same content authority used by public pages.
- Live Windows target frontend `npm run typecheck` and `npm run build` PASS. Focused real Edge Playwright Platform Administration 1/1 PASS with explicit cross-tab rendered-content assertions; the project-owned disposable MySQL service returned to its initially stopped state after test teardown. No real Software entry, policy or external integration was activated.
- This is a protected **content** preview, not an exact Next.js public-route screenshot. Full release-note/version-detail, SEO/media/public route equivalence and an authenticated Admin-to-Next.js browser journey remain separately unaccepted under MT-7.5; do not use this partial preview to close that whole requirement or the point.

## Independent multi-Software tenant-like isolation check

- Added an authenticated second synthetic Software Product in the joined Admin/Public API feature test. The second product is absent before its own publication, has an independent Overview and no inherited releases, rejects silent collision with the first protected slug, and cannot be archived by a content-only editor. Publisher archive hides only the second public route while preserving its revision history and leaving the first Product/Release public and unchanged.
- Fresh project-owned isolated MySQL on `127.0.0.1:13306`: focused test PASS 1/1 (50 assertions), PHP syntax and diff whitespace PASS; isolated target MySQL restored to its initially stopped state. This change is not included in CI `35440322038` and requires separate clean-checkout CI on the exact future revision before remote acceptance. No production Software publication or external operations.

## Software administration permissions and policy-draft integrity

- UI review found Software `Archive` and `Change canonical slug` enabled for content editors even though the existing service requires `website.publish`. Align those two buttons to publisher permission without changing or weakening backend access checks; mock-browser editor/publisher contrast and real HTTP editor-archive denial provide regression evidence.
- The legal-policy form also silently sent `owner_approved` and `verified` whenever a policy draft was saved. That would falsely imply separate human and factual review when used by an authorized publisher. The normal content form now saves only `approval_state=draft` and `factual_review_state=pending` and identifies the review/publication gates as separate. Authenticated content-only editor cannot forge approved/verified state (HTTP 403). No owner/legal decision, policy effective-date approval, or real publication is inferred. An explicit auditable authorized approval workflow and final policy decisions remain unaccepted under MT-7.5.
- Target-only MySQL PHP feature check and Windows Edge Platform Admin UI test must PASS on this exact change before commit; clean-checkout GitHub CI on the eventual candidate is independently required. No production policy/customer record or external provider touched.

- Local correction check on 19-Sep-2026: `php -l` PASS; backend typecheck and production Vite build PASS; target-only isolated MySQL Software/policy HTTP feature 1/1 PASS (54 assertions), real Edge Playwright Platform Administration 1/1 PASS (editor/publisher UI and unreviewed draft payload), and MySQL restored to initially stopped state. `git diff --check` PASS. The current remote multi-Software run `35440706244` does not contain these additional UI/legal changes; exact new-candidate remote CI remains pending.

- Further policy-review UI acceptance: the protected Policy revisions `Publish` button now stays disabled unless the draft records `owner_approved` AND `verified`; backend recent-auth, decisions and permission validation still independently applies, and a browser fixture proves both pending-disabled and reviewed-enabled UI states. This only aligns visible controls with preexisting backend review gates; it never fabricates an owner/legal sign-off or activates any actual publication. Local backend typecheck/production build PASS, targeted Edge Playwright 1/1 PASS, disposable MySQL restored to initially stopped state. This patch is local until prior exact-candidate CI reaches terminal state and a fresh clean-checkout candidate verifies it.

- Remote acceptance of the preceding legal-draft and Software publisher-only control correction: GitHub run `35441098389`, job `105891906466`, exact `8d7c3c07e6ec07f906ec3b3ee55c1795e161f0c6`, terminal SUCCESS across every clean-checkout gate. This result does not include the later Policy Publish-button pending-review disablement, which separately passed the local rebuilt Edge test and still needs a fresh remote run on its own exact commit. Neither local nor CI fixtures constitute owner/legal approval or real Software/policy publication.

## Release-note preview parity extension

- The protected Software Admin response now loads existing release `notes` and `impact_review` from the same revision-scoped release records used by the public renderer, decoding both JSON fields for the authenticated administrator only. The private Software content preview renders grouped Added/Changed/Fixed/Security notes and a review-field count alongside version, date, publication state and summary; it does not expose an unauthenticated draft URL or publish content. The count is an indication of stored review entries, not an approval verdict.
- The synthetic joined HTTP regression asserts persisted release-note and review data in the protected Admin response; the browser fixture checks rendered draft release-note groups. Local focused isolated MySQL test PASS (1/1, 57 assertions); initial test DB stopped state restored. The initial browser check used a stale pre-change Vite build and failed specifically to locate a newly introduced note; the rebuilt application must be used for browser and fresh clean-checkout CI acceptance.
- Exact Next.js public-route screenshot equivalence, owner-approved real release documents, full source parity and production publication remain unaccepted/HOLD. MT-7.5 remains In Progress; MT-7.6 not started.
- Rebuilt production Vite bundle and reran the same actual Edge browser scenario: 1/1 PASS, including protected Added/Fixed release notes and impact-review count. The first failure is superseded by this evidence-based build correction, not counted as a PASS. Target-only test MySQL returned to its initially stopped state. Fresh remote CI on the exact committed release-preview candidate is still required.

## Real authenticated Admin to Next.js Software revision acceptance

- Added `backend/tests/browser-website/dynamic-content.spec.ts` MT-7.5 journey. A genuine seeded test-only Admin session opens the protected React Software editor, previews a private revision, publishes it, saves a later private draft, publishes that revision and rolls back to the original history. Each transition is checked independently through the real Next.js public Software page, with the unpublished revision absent before its publish. Existing version `1.0.0` remains visible after rollback. No mocked Admin routes, private source modification, real policy or production Software publication.
- Initial focused browser attempts showed an asynchronous UI/HTTP synchronization gap (checking the newly rendered revision before the POST/reload completed). Exact mutation-response status and loaded revision are now awaited; no backend validation or permission gate was bypassed. The later rollback exposed a real test-only cleanup FK restriction: the synthetic Software revision history has nullable `restored_from_revision_id` references and could not be deleted as a group. `DynamicWebsiteE2eCleanupSeeder` now nulls this link only for the preidentified synthetic `mt54-software` product in the strictly `mobisttech_test` database before scoped deletion. No foreign key, schema constraint or production deletion guard was relaxed.
- Focused isolated Windows Edge/real Laravel+Next.js run PASS 1/1 (54.7s browser execution); both Playwright afterAll and global teardown cleanup PASS, previous project-owned MySQL stopped state restored. Exact clean-checkout CI for this new browser case and all sibling tests remains required. Other Software media/SEO/public-route visual equivalence, original-source families, owner/legal approval and provider activation remain pending. MT-7.5 In Progress; MT-7.6 not started.

## Exact-candidate CI infrastructure block and local full-browser evidence

- GitHub application candidate `21af94a6cbb1e32039087a83da650a3dd8b5b023` was pushed to canonical `main` with clean working tree and remote SHA match. Manual workflow run `35442617990`, job `105895966143`, terminal **failure before job setup**, `steps=[]`, `runner_name` empty, with GitHub annotation: "The job was not started because recent account payments have failed or your spending limit needs to be increased. Please check the 'Billing & plans' section in your settings". This is a GitHub billing/spending-limit barrier, not an executed application-test failure; do not retry the same blocked job until authorized billing configuration is resolved. No billing or account limits were changed.
- Independently ran the complete `backend/tests/browser-website/dynamic-content.spec.ts` suite against rebuilt real local Laravel+Next.js, project-owned `mobisttech_test` and Edge: **3/3 PASS**, including existing CMS/digital/Software public-page fixtures, commerce-only mode, and authenticated Admin revision publish/update/draft-isolation/rollback to the real Next.js route. Website test afterAll/global teardown PASS, isolated MySQL returned to its initially stopped state. This local evidence does **not** substitute for fresh clean-checkout CI or full source-family and production/legal sign-off. MT-7.5 remains In Progress; MT-7.6 not started.

## Four-way POS payment allocation / Website separation (local evidence, remote gate HOLD)

- Added `PosPaymentTest::test_all_four_pos_methods_share_one_invoice_and_keep_cash_closing_and_website_channels_distinct`: a real target-only MySQL sale allocates one invoice across cash PKR 20.00, card PKR 50.00, mobile wallet PKR 60.00 and bank transfer PKR 70.02; cash tendered PKR 30.00 returns PKR 10.00 change. It checks exactly one invoice/four allocations, cash closing counts only PKR 20.00, all three non-cash destinations stay distinct, and the Website config remains exactly COD/JazzCash/Easypaisa/card (no Website bank transfer).
- Isolated Windows MySQL 8.4 `mobisttech_test` focused PASS 1/1 (14 assertions); joined PosPayment/CashSession/DocumentReportingServices/OrderPaymentTransactions PHP PASS 21/21 (183 assertions). Test runner returned the project-owned database to its previously stopped state; production/provider records untouched. This is backend integration evidence only; UI four-way live HTTP, full source comparison, provider checkout and GitHub clean-checkout acceptance remain pending. Exact CI on latest joined-Software candidate remains blocked **before runner startup** by GitHub billing/spending-limit rejection; no unchanged-run retry or fabricated acceptance.
