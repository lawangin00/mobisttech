# MT-7.5 W04 — hosted browser failure isolation (21 Sep 2026)

**Status:** diagnostic checkpoint only. No browser test was changed or rerun at this checkpoint. W04 remains IN PROGRESS; the parity checklist remains 15/27 DONE, 12 OPEN. No real payment provider is enabled or accepted.

## Exact failing source and independent passing gates

- Clean-hosted run: https://github.com/lawangin00/mobisttech/actions/runs/35628330989, checked out source commit `7df4142a0909667cbc91e738669c81b5542fcba3` (the exact-source request for `89a5a66d3e2516bd0152009c00788657d3b71a10`). The request gate, Pint (276 PHP files), full backend regression (341 tests), MySQL race/reset (23 tests), Website build, public budgets and secret scan passed. The full run nevertheless finished FAILURE at the combined POS/Admin Playwright gate: 19 failed, 15 passed, 1 skipped; downstream Website browser and final schema gates did not execute.
- First failure: `backend/tests/browser/mt75-fresh-next-smoke.spec.ts:63`. `getByRole('button', { name: /MT75 Fresh Accessory/ })` resolves to the product-selection button containing only the name and code, then incorrectly expects that button to contain `Qty 0` (the next stock assertion for `Qty 3` uses the same wrong button locator). The second product has the same stale assertion for `Qty 0` and `Qty 1`.
- The actual React inventory card in `backend/resources/js/components/pos-transaction-workspace.tsx` renders its product-selection `<button>` and a separate sibling `<p>Qty {p.qty}</p>` in the same product-card `<div>`; quantity is not a button descendant. A bounded correction should select the card containing that exact product button, then assert its own visible quantity paragraph (`Qty 0`, `Qty 3`, `Qty 0`, `Qty 1`). Do not change production markup merely to satisfy the stale test or assert stock from another card.
- `backend/playwright.config.ts` runs the entire `tests/browser` directory serially (`workers: 1`, no retries). Its global setup installs `FirstOutletE2eSeeder` and `PosShellE2eSeeder` in a shared `mobisttech_test` schema. The early failed fresh-publication test already created an outlet and accessory product before line 63. A later `pos-first-outlet-fresh-owner.spec.ts` uses the same synthetic owner and expects no outlet; `PosMasterDataLinkedE2eSeeder` explicitly failed because the accessory baseline was no longer empty. These are demonstrable fixture-precondition conflicts, not evidence that the payment transaction feature tests failed.
- The next fresh-customer scenario immediately expects the product published by the preceding failed scenario; its missing heading is a downstream missing-prerequisite failure. Other Admin-login timeouts, customer login HTTP 422, sitemap/warranty seeder failures and residual `identity_audit_events:1` were observed but their causes are not independently confirmed by this static inspection; inspect the individual response/error contexts before modifying identity or cleanup security.

## Bounded next action

1. Correct **only** the four quantity assertions in the complete existing `mt75-fresh-next-smoke.spec.ts`, preserving every other line and transaction assertion; review the exact commit diff. This GitHub-only connector exposes complete-file replacement, so never submit a truncated or reconstructed partial file.
2. Arrange genuinely isolated fresh-owner/fresh-publication browser prerequisites rather than deleting business history or bypassing seeder guards. Run the smallest allowed clean hosted browser scope on that exact source. In particular, do not count a later consumer test as independent when it relies on an earlier test having completed.
3. Once isolated failures are resolved, verify the remaining browser/schema gates and resume W04's 52-source-file / 16-source-route payment crosswalk. Existing W03 closure and authentic-provider HOLD remain unchanged.
