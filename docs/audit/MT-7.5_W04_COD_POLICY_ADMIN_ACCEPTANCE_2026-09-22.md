# MT-7.5 W04 — protected nonsecret COD policy and Admin interface (22-Sep-2026)

**Status: scoped implementation/HTTP/static and full clean-checkout CI PASS; W04 remains IN PROGRESS (15/27 DONE, 12 OPEN).** This checkpoint does not close W04, approve an external merchant or establish a new dedicated real-browser COD policy interaction/rollback test.

## Implemented boundary

- Source candidate `6d1caaf5b401ca12762671f636e8a756423ef612` contains `WebsitePaymentAdministration`, a distinct Admin-only COD draft/publish route pair, and the permission-scoped Inertia `website-payment-settings` page. Existing GET `/internal/admin/website/payment-settings` and GET `/internal/admin/website/payment-channels` remain masked, no-store and read-only; no mutation verb was added to either status path.
- `website.payments.manage` is checked by the service for reading and drafting; `website.publish` is independently required before publishing. Draft writes strictly accept one nonsecret boolean `cod_enabled`, reject unexpected merchant/credential/configuration fields, and do not change checkout. Explicit publish records an audited revision and applies only the COD-off/on policy. Stale/previously published revision replay is rejected; configuration-level disabled COD cannot be overridden by a policy. Current order and prior payment records are not cancelled by the policy.
- The authoritative `PaymentProviders` checkout availability applies the published COD policy without configuring, registering, activating or changing JazzCash/Easypaisa/hosted-card adapters. All external provider configurations remain default OFF and real merchant credentials are absent.
- Synthetic `W04CodPolicyAdministrationHttpTest` proves guest, insufficient permission, unknown payload/key, invalid boolean, draft isolation, distinct publish permission, stale revision rejection, COD disable/reenable and unchanged external-provider configuration. The Admin form renders a current COD setting, pending draft and gated publish control, alongside the existing four masked channel statuses; the new control has not yet had its own dedicated real-browser click/reload/rollback test.

## Verified retry record, exact source, and terminal result

1. Full run `35756936587`, request source `551cd214a612d04e7bc5f2e0bdc72f4330269a87`: FAILED only on the new test's initial guest POST (correct framework CSRF 419 before its expected 401). 356 existing backend tests and style/backend build passed; later CI gates skipped. Material test-only change `80339952fae72414fa52103d9c3e0e4cc07902e6` first establishes the unauthenticated client's real CSRF cookie, then tests its 401 without bypassing CSRF.
2. Full run `35757646246`, request source `80339952fae72414fa52103d9c3e0e4cc07902e6`: FAILED only at the new test's sixth common-draft POST (correct identity throttle HTTP 429, not an authorization/product failure). Existing production limiter is 5 requests per minute per realm/path/identity/IP; four independent synthetic actor clients had accidentally shared one IP. Material test-only change `6d1caaf5b401ca12762671f636e8a756423ef612` assigns each independent synthetic client its own stable loopback REMOTE_ADDR while retaining normal identity/CSRF throttle middleware, limits and all security assertions.
3. Third and final corrected full request-only commit `c0e5f3e0006223868a34b7ddd5cccd7368762aff` pins **exact source `6d1caaf5b401ca12762671f636e8a756423ef612`**, correct project identity, stage MT-7.5, `reason=necessary`, `gate=full`. [Full clean-checkout run 35758440858](https://github.com/lawangin00/mobisttech/actions/runs/35758440858) **terminal SUCCESS**: request validator, PHP style, backend TypeScript/build, complete backend regression including corrected new W04 test, explicit MySQL race/reset, Website typecheck/lint/build, public/secret budgets and tracked-secret scan, fresh-owner browser, POS/Admin Playwright, Website checkout/customer/project/public browsers, Website performance and final schema/cleanup/no-tracked-artifact gates all succeeded. The separately scoped W01/P02/W03 isolated browser gates were intentionally skipped in this full-scope request; no new claim of their focused verification is made here.
4. [Independent Website-only run 35758440865](https://github.com/lawangin00/mobisttech/actions/runs/35758440865) **terminal SUCCESS** for exact-source request validation and clean Website TypeScript, lint and production build. This supplements, not replaces, the full CI browser result.

## Remaining boundary and first next action

W04 is still open: implement and independently browser-verify the real protected COD draft/publish/reload and rollback journey with direct permission denials and publish-race/invalid-revision negative checks, then continue missing approved external-provider owner/credential-management design only after genuine contracts/identity/authorization exist. No source legacy database or protected old repo was mutated. Genuine JazzCash/Easypaisa/hosted-card merchant identity, official adapter contracts, real callbacks, refunds and bank settlement remain **H-02 EXTERNAL UNVERIFIED/HOLD**; synthetic CI cannot establish any of these. COD is the only available Website checkout channel under the default configuration.
