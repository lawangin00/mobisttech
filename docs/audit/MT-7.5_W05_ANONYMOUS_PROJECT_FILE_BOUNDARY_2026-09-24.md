# MT-7.5 / W05 anonymous project and private-file denial (24-Sep-2026 PKT)

Status: bounded LOCAL browser acceptance only, W05 OPEN [E], MT-7.5 15/27 DONE / 12 OPEN. Parent source `59c2b9f`; no application/API/session-policy edits, no provider activation or hosted CI.

- In the already accepted real Admin -> Customer private-delivery journey, use a **new, cookie-free browser context** against the exact owned project API URL and just-delivered file URL. Both authenticated owner download and real protected Admin upload still succeed, while each corresponding unauthenticated proxied API request must return 401/403; neither a rendered public Next.js project page nor an HTTP 200 shell is counted as a protected API read. Only synthetic, browser-fixture-owned identities and objects are used.
- First scoped attempt failed 1/2 because the assertion requested `/account/projects/{id}`, whose public Next.js **HTML shell** legitimately returned HTTP 200. Classified FIXTURE / wrong endpoint; corrected the request to the actual authenticated `/api/customer/projects/{id}` REST proxy, without changing backend authorization or retrying an unchanged failure. The targeted real Edge test then passed **1/1** in 34.4 seconds with guarded Playwright global fixture setup/teardown. This tests unauthenticated denial, not yet foreign-authenticated Customer denial.
- The preceding `59c2b9f` real Admin -> Customer and W04 adjacent Website-browser suite was 6/6 PASS on unchanged application/browser fixture/config. This checkpoint changes only one additive guest-negative assertion and preserves every earlier browser journey; the focused 1/1 is the new proof and the unaffected 6/6 result is reused, not automatically rerun. Prior `ClientProjectServicesTest::test_customer_project_api_is_owner_scoped_and_exposes_only_configured_project_payment_channels` establishes foreign-customer project GET and reference POST denial only at PHP HTTP scope; foreign-account real-browser and final family joined closure remain separately evaluable.
- All checks ran locally on the disposable `mobisttech_test@127.0.0.1:13306` via the cooperative exclusive runner after no-other-runner/no-lock preflight; guarded synthetic setup/teardown completed. The wrapper cannot prove entire-schema residue or monitor direct runner calls; none is claimed. W05 status/expiry/mode complete joined acceptance remains OPEN; W04 provider-specific H-02 and Q01 final gates remain separate. No production or protected historical source data was touched.