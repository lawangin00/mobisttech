# MT-7.5 / W05 development-family closure (24-Sep-2026 LOCAL)

**Formal scope:** stable `18/W05` ORIGINAL source-functional family, not MT-7.5 stage exit, H-02 provider acceptance, finished Website publication/legal copy, all project requirements or FINAL-AUDIT. The prior 24-file/9-route target dispositions are in `MT-7.5_W05_SOURCE_BEHAVIOR_DISPOSITION_2026-09-24.md`; no original source rows, identities or public bearer tokens are imported. Existing W04 development completion and the external H-02 PRE-LAUNCH HOLD remain unchanged. Parent application SHA `f18c31a6c9a8a8e9842c3d63ca85388efc95a07a`; this change is test fixture/browser evidence and project docs only, not business product logic.

| Finite W05 DEVELOPMENT gate | Exact accepted evidence and limits |
|---|---|
| W05-1 New public service enquiry and private protected lead, pricing/optional consent and consultation boundaries | Real public `/enquiry` -> Laravel lead -> protected unmocked Admin UI/HTTP -> project creation: existing `client-project.spec.ts` joined sixth case. Published services/package selection and new enquiry: existing real `dynamic-content.spec.ts` scoped acceptance. Isolated PHP `DigitalServiceLeadsTest` verifies exact package/add-on snapshots, optional consultation choice, safe reference file and role/mode/rate/replay safeguards. Account opt-in marketing belongs to already accepted Customer engagement; never infer marketing consent from a project enquiry. Public anonymous lead-file upload is not claimed; approved private client reference exchange is through authenticated project portal and server-side safe file contract. |
| W05-2 Proposal/quote approval, exact milestone and expiry | Real Admin proposal -> Customer public account PKR 10000.00 approved amount and scope; actual owned history across commerce-only mode. `ClientProjectServicesTest` confirms immutable proposal/quote/milestone sums, tampering/replay/expired approval denial, and fresh-session owned historical GET of expired approved quote with `payable=false` in both digital-only and commerce-only. Expired quote's new milestone POST returns 409 without order/payment/idempotency rows, using a synthetic adapter to distinguish H-02 external-provider OFF from the internal expiry guard. No actual bank/provider payment is inferred. |
| W05-3 Project state and paid/closed constraints | Real, unmocked Admin React/API `approved -> in_progress -> review -> delivered`, unpaid `completed` PATCH 409, permitted `closed` PATCH 200 and Customer closed history/no further reference upload; immutable events and historical terms verified across mode. Existing PHP service test proves **synthetic** paid-all-milestones `completed` and rejects unpaid completion; genuine provider receipt is H-02 and not W05 development PASS. |
| W05-4 Private files, Customer ownership, source historical bearer-token retirement | Real authorized Admin delivery -> owned Customer private download; anonymous project/file GET denied. NEW independent signed-in unrelated Customer via exact guarded disposable fixture receives empty project list and 404 for other Customer's project/private file in `hybrid`, `commerce_only` and `digital_only`, while original owner retains HTTP 200 historical project access. Existing PHP 409 foreign milestone POST and no new order/payment, owner-bound upload, signed/history and forbidden legacy public token access remain accepted on unchanged backend contracts. Nothing is exposed as an anonymous project-payment bearer route. |
| W05-5 Full applicable family join, style, traceability | Source 24/24 files and 9/9 routes disposition reused, not remapped. Exclusive sequential local real Microsoft Edge `backend/tests/browser-website/client-project.spec.ts` **7/7 PASS** (2.1m, terminal exit 0; includes impacted W04 digital-payment neighbors, full global fixture teardown); isolated `ClientProjectServicesTest` + `DigitalServiceLeadsTest` **10/10 PASS, 103 assertions**, terminal exit 0. Changed new seeder PHP syntax and Pint --test PASS; static project efficiency policy and Git diff check required at final commit. Unchanged existing actual `dynamic-content.spec.ts` 3/3 and W04 117/117 previously accepted evidence reused, not rebranded as exact-current full-hosted CI. |

**Failure and fixture integrity:** Initial foreign-Customer real Edge case executed but **was NOT accepted** because `afterAll` encountered SQLSTATE 1451: the real account UI had created that exact synthetic actor's default `notification_preferences` row, which the new test-only seeder had not removed. Classified `FIXTURE`, not product defect. Verified exact foreign account identity and one default preference; implemented transactionally guarded, account-scoped cleanup with immutable default-preference assertion, negative business-state guards and a valid schema check (no imaginary column on `notification_delivery_attempts`). Scoped corrective cleanup succeeded, the focused Edge test **1/1 PASS**, and the later full real Edge join **7/7 PASS** with foreign fixture cleanup and global teardown. No unowned/business user, original source project or real gateway was touched.

**Separate Q01 clean-candidate limitation (NOT a W05 application-family claim):** Read-only LOCAL disposable-schema diagnostic after the joined suite reported `service_request_rate_buckets:3` (DB bucket hours `2026-09-24 15:00`, `16:00`, `17:00`; counts `5`, `4`, `3`), with zero open service requests and the exact foreign account absent. These are anonymous/IP+agent rate-limit buckets spanning prior test periods; there was no pre-run provenance proving which pre-existing buckets belonged to this run, so none was deleted or silently declared owned. Real browser/full family and named fixture teardown were PASS, but **database-wide zero-residue/Q01 exact-current-candidate acceptance is OPEN**. Before final Q01 clean-checkout, establish an independently owned disposable baseline or evidence-backed rate-bucket fixture ownership/cleanup and prove zero residue without purging ambiguous records. This is a test-harness/clean-schema verification concern, never permission to disable production rate limiting or waive Q01.

**Formal outcome:** `18/W05` DEVELOPMENT DONE with the approved account-owned target behavior and the preceding non-HOLD family gates evidenced; MT-7.5 development index **17/27 DONE, 10 OPEN**. H-02 official merchant contracts, sandbox identities/credentials, genuine callbacks/refunds/settlements and separate channel/launch approval stay PRE-LAUNCH PENDING with external channels OFF. `19/W06` is the next independently executable family; do not reopen W04/W05 without a demonstrated regression. MT-7.5/Q01/MT-7.6/final audit/project 100% remain unclaimed.