# MT-7.5 W07 Website credential safety checkpoint (25-Sep-2026 LOCAL)

Scope: independent Website-specific six-key allowlist from the protected read-only registry, installed ONLY under the authorized fresh target `C:\mobisttech`. This is encrypted configuration and **does not activate any external provider**; no actual business credentials, source database, remote sandbox or production secrets were accessed. W06 and earlier accepted W07 theme/media/branding scopes remain unchanged.

- `backend/app/Cms/WebsiteSecretSettings.php`: six allowlisted Website provider credential names, encrypted Laravel `Crypt` envelope, versioned replacement after decrypting prior active envelope (foreign/corrupt key denies), explicit `website.payment-credentials.manage` rights, audit with key identity only. Metadata masks the **entire** saved value `********`, exposes no trailing secret bytes, decrypted material or ciphertext. Undefined keys and invalid values denied. This is a separate namespace from POS destinations, OAuth tokens and hosted payment activation.
- Protected `PUT /internal/admin/platform/website-credentials/{key}` additionally requires existing CSRF, Admin identity, recent human authentication and throttling. `/platform/data` projects safe credential status ONLY for authorized Admins; unauthorized staff receive no credential status. `website-secret-editor.tsx` uses a password-only input, never prepopulates it, clears it after confirmed success and never fetches stored values.
- `WebsiteCredentialSecurityTest.php`: isolated protected target PHP **1/1 PASS (16 assertions)** `.local/w07_secret_foreignkey.log` verifies encryption, masked status, two versions, recovery manifest includes neither plaintext nor ciphertext, stale captured secret state denied after rotation, foreign-key and invalid envelopes cannot be read or overwritten. `PlatformAdministrationInterfaceTest.php` affected HTTP + existing read-scope tests and `OperationalRecoveryTest.php` recovery assertions ran **4/4 PASS (81 assertions)** `.local/w07_credentials_affected_php.log`; the HTTP test checks recent-auth expiry/reconfirmation, scope, invalid routes and request/response non-disclosure.
- Actual protected Owner browser workflow `theme-w07.spec.ts -g 'W07 protected credentials'`: **1/1 Edge PASS (46.9s)** `.local/w07_credential_edge_initial.log`. Private admin replacement returns only masked metadata and clears input; protected data and actual public Next home/content response never expose the submitted synthetic value. `W07CredentialE2eBaselineSeeder` refuses preexisting provider credentials; `W07CredentialE2eCleanupSeeder` deletes only the specific owner-authored, exact-value/version synthetic envelope and own audit event in the disposable `mobisttech_test@127.0.0.1:13306` schema. Global setup and teardown completed.

No direct credential reveal, historical secret restore, live gateway execution or provider activation is claimed or permitted. Current W07 complete joined family acceptance and development closure remain OPEN until separately terminal-verified; earlier failed 2/3 joined attempt is NOT credited as PASS.
