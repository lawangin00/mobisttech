# H01 bounded P08 original backup/operations route disposition (25-Sep-2026 LOCAL)

Original POS P08 has **19 source routes**: 18 new to H01 path-specific review and one `super-admin/control-center/activity` already disposed in the approved 54-route P01 crosswalk. P08 previously DONE for guarded backup/operations *development readiness*; that accepted target service/isolated-recovery evidence is retained, not rerun. This table distinguishes actual exposed target routes from backend-only services; **do not treat a service method as an authenticated browser/API route**. No original separate Admin/Shop/SuperAdmin credential realm, source data, real cloud backup or destructive restore is activated.

| # | Pinned original method / URI | Reused target service / current exposed route or explicit gap | Disposition evidence boundary |
|---:|---|---|---|
| 01 | `GET\|HEAD admin-panel/backups` | Protected GET `/internal/admin/integrations/google_drive/backups` now exposes the existing bounded 50-item `BackupService::history` only to `backups.manage`; protected `/internal/admin/settings/integrations` renders the list even for backup-only Admin without revealing connection status or credentials. | H01 TARGET-ONLY ACCEPTED: focused protected role and redacted history PHP 1/1 (57 assertions), actual Edge 2/2 on synthetic fixture; no real cloud/production proof. |
| 02 | `POST admin-panel/backups` | Protected `POST /internal/admin/integrations/google_drive/backup-now` -> `BackupService::request` exists for allowed connected Google Drive; original Shop/Admin/SuperAdmin duplicate forms/credentials retired. | REUSE existing P08/W08 synthetic status/disabled-service proof; activation and authentic remote remain independent external HOLD. |
| 03 | `DELETE admin-panel/backups/{backup}` | Protected recent-auth+CSRF DELETE `/internal/admin/integrations/google_drive/backups/{backup}` calls new **local-only** `deleteLocal` guard; queued/processing, remote-bound and missing files fail closed. Remote copy and retained record remain intact; no external API deletion is exposed by the UI. | H01 TARGET-ONLY ACCEPTED for **local-only** delete and audit-preserving record; remote deletion is deliberately not exposed pending external/owner activation. |
| 04 | `GET\|HEAD admin-panel/backups/{backup}/download` | Protected GET `/internal/admin/integrations/google_drive/backups/{backup}/download` reuses `BackupService::readLocal` completed-business/private-path guards and sends explicit attachment with private/no-store/nosniff; only local retained copies have a UI download control. | H01 TARGET-ONLY ACCEPTED for completed private local downloads, 404 remote-only/no-local, no secret/path projection; no provider request performed. |
| 05 | `GET\|HEAD admin-panel/control-center/backup-operations` | Protected `GET /internal/admin/settings/integrations` renders current Google Drive backup schedule/retention and last backup status; old multi-realm control page retired. | Current React `integrations.tsx` and backend safe projection; reuse existing service/backend tests. |
| 06 | `PUT admin-panel/control-center/backup-operations` | Protected `PUT /internal/admin/integrations/google_drive/backup-settings` -> `IntegrationController::backupSettings` -> `IntegrationManager` with role + recent-auth. Old source separate database and credential setup retired. | Reuse previous W08/P08 integration controls; no real external backup or source config migration. |
| 07 | `GET\|HEAD admin-panel/control-center/integration-status` | Protected `GET /internal/admin/settings/integrations` (`IntegrationController::page`) and `GET /internal/admin/integrations` (`IntegrationManager::statuses`) display masked configured provider/backup status. | Accepted shared W08/P08 readiness; separate legacy route retired. Real provider health unverified. |
| 08 | `GET\|HEAD shop/backups` | Protected GET `/internal/admin/integrations/google_drive/backups` now exposes the existing bounded 50-item `BackupService::history` only to `backups.manage`; protected `/internal/admin/settings/integrations` renders the list even for backup-only Admin without revealing connection status or credentials. | H01 TARGET-ONLY ACCEPTED: focused protected role and redacted history PHP 1/1 (57 assertions), actual Edge 2/2 on synthetic fixture; no real cloud/production proof. |
| 09 | `POST shop/backups` | Protected `POST /internal/admin/integrations/google_drive/backup-now` -> `BackupService::request` exists for allowed connected Google Drive; original Shop/Admin/SuperAdmin duplicate forms/credentials retired. | REUSE existing P08/W08 synthetic status/disabled-service proof; activation and authentic remote remain independent external HOLD. |
| 10 | `DELETE shop/backups/{backup}` | Protected recent-auth+CSRF DELETE `/internal/admin/integrations/google_drive/backups/{backup}` calls new **local-only** `deleteLocal` guard; queued/processing, remote-bound and missing files fail closed. Remote copy and retained record remain intact; no external API deletion is exposed by the UI. | H01 TARGET-ONLY ACCEPTED for **local-only** delete and audit-preserving record; remote deletion is deliberately not exposed pending external/owner activation. |
| 11 | `GET\|HEAD shop/backups/{backup}/download` | Protected GET `/internal/admin/integrations/google_drive/backups/{backup}/download` reuses `BackupService::readLocal` completed-business/private-path guards and sends explicit attachment with private/no-store/nosniff; only local retained copies have a UI download control. | H01 TARGET-ONLY ACCEPTED for completed private local downloads, 404 remote-only/no-local, no secret/path projection; no provider request performed. |
| 12 | `GET\|HEAD super-admin/backups` | Protected GET `/internal/admin/integrations/google_drive/backups` now exposes the existing bounded 50-item `BackupService::history` only to `backups.manage`; protected `/internal/admin/settings/integrations` renders the list even for backup-only Admin without revealing connection status or credentials. | H01 TARGET-ONLY ACCEPTED: focused protected role and redacted history PHP 1/1 (57 assertions), actual Edge 2/2 on synthetic fixture; no real cloud/production proof. |
| 13 | `POST super-admin/backups` | Protected `POST /internal/admin/integrations/google_drive/backup-now` -> `BackupService::request` exists for allowed connected Google Drive; original Shop/Admin/SuperAdmin duplicate forms/credentials retired. | REUSE existing P08/W08 synthetic status/disabled-service proof; activation and authentic remote remain independent external HOLD. |
| 14 | `DELETE super-admin/backups/{backup}` | Protected recent-auth+CSRF DELETE `/internal/admin/integrations/google_drive/backups/{backup}` calls new **local-only** `deleteLocal` guard; queued/processing, remote-bound and missing files fail closed. Remote copy and retained record remain intact; no external API deletion is exposed by the UI. | H01 TARGET-ONLY ACCEPTED for **local-only** delete and audit-preserving record; remote deletion is deliberately not exposed pending external/owner activation. |
| 15 | `GET\|HEAD super-admin/backups/{backup}/download` | Protected GET `/internal/admin/integrations/google_drive/backups/{backup}/download` reuses `BackupService::readLocal` completed-business/private-path guards and sends explicit attachment with private/no-store/nosniff; only local retained copies have a UI download control. | H01 TARGET-ONLY ACCEPTED for completed private local downloads, 404 remote-only/no-local, no secret/path projection; no provider request performed. |
| 16 | `GET\|HEAD super-admin/control-center/activity` | Already disposed in `MT-7.5_P01_ROUTE_CROSSWALK.md` row 39 -> protected `/internal/admin/pos/audit` read-only redacted viewer. | REUSE P01 exact source-route mapping, not another H01 test. |
| 17 | `GET\|HEAD super-admin/control-center/backup-operations` | Protected `GET /internal/admin/settings/integrations` renders current Google Drive backup schedule/retention and last backup status; old multi-realm control page retired. | Current React `integrations.tsx` and backend safe projection; reuse existing service/backend tests. |
| 18 | `PUT super-admin/control-center/backup-operations` | Protected `PUT /internal/admin/integrations/google_drive/backup-settings` -> `IntegrationController::backupSettings` -> `IntegrationManager` with role + recent-auth. Old source separate database and credential setup retired. | Reuse previous W08/P08 integration controls; no real external backup or source config migration. |
| 19 | `GET\|HEAD super-admin/control-center/integration-status` | Protected `GET /internal/admin/settings/integrations` (`IntegrationController::page`) and `GET /internal/admin/integrations` (`IntegrationManager::statuses`) display masked configured provider/backup status. | Accepted shared W08/P08 readiness; separate legacy route retired. Real provider health unverified. |

## H01 local operator acceptance supersession (25-Sep-2026; current candidate)

The original source backup-history/download/delete *local operator paths* now use one unified protected Admin UI. `docs/migration/FEATURE_PARITY_REGISTER.md` explicitly required retaining backup history/download/delete rights. Existing P08 recovery/service PASS was reused without rerunning its unchanged suite. New isolated `H01BackupOperatorTest` **1/1 PASS (57 assertions)** covers guest/non-owner denial, backups-only Admin page with no integration-status/credential access, 50-record bounded history, completed-only safe private attachment, remote-only 404, remote deletion 409, CSRF 419, exact local deletion and retained audit. Actual Microsoft Edge joined **2/2 PASS** from guarded `H01BackupBrowserSeeder` synthetic completed local copy: rendered responsive 390px page, actual file download, confirm/delete, retained row, forbidden salesperson and lazy-loaded Platform Admin. First browser **1 PASS/1 FAIL** was a test-only status-message locator that looked inside the history section; network deletion already returned 200; corrected locator and final 2/2 PASS after the later split-page build. All owned fixture records/files/audit and owner sessions cleaned; exact-PID test server stopped; unrelated target MySQL initial Running state preserved. No real backup, remote deletion, payment/provider, production, protected source or customer data touched. Current backend public bundle budget PASS after separate lazy Inertia page chunks.

Historical source Shop/Admin/SuperAdmin credential duplication remains retired; current `backups.manage` is the only backup operator authority. Authenticated remote backup download/deletion and actual provider execution remain separate external/owner HOLD, not part of this target-only local-copy acceptance. This 19/19 P08 route disposition is evidence for H01 only; does not reopen prior accepted P08 or imply Q01 final milestone.
