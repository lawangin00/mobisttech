# H01 bounded P08 original backup/operations route disposition (25-Sep-2026 LOCAL)

Original POS P08 has **19 source routes**: 18 new to H01 path-specific review and one `super-admin/control-center/activity` already disposed in the approved 54-route P01 crosswalk. P08 previously DONE for guarded backup/operations *development readiness*; that accepted target service/isolated-recovery evidence is retained, not rerun. This table distinguishes actual exposed target routes from backend-only services; **do not treat a service method as an authenticated browser/API route**. No original separate Admin/Shop/SuperAdmin credential realm, source data, real cloud backup or destructive restore is activated.

| # | Pinned original method / URI | Reused target service / current exposed route or explicit gap | Disposition evidence boundary |
|---:|---|---|---|
| 01 | `GET\|HEAD admin-panel/backups` | `BackupService::history(Admin)` exists and checks `backups.manage`, but **no protected Admin backup-history route/UI found** in current `routes/identity.php`/`integrations.tsx` (current status displays only last backup). | OPEN [I/E]: verify required per-backup browsing contract and decide bounded authenticated history UI/API or approved explicit retirement with evidence. |
| 02 | `POST admin-panel/backups` | Protected `POST /internal/admin/integrations/google_drive/backup-now` -> `BackupService::request` exists for allowed connected Google Drive; original Shop/Admin/SuperAdmin duplicate forms/credentials retired. | REUSE existing P08/W08 synthetic status/disabled-service proof; activation and authentic remote remain independent external HOLD. |
| 03 | `DELETE admin-panel/backups/{backup}` | `BackupService::delete(Admin, backupRecordId)` already checks permission/safe local path/remote status, but **no protected Admin HTTP delete route/UI found** in current routes/React. | OPEN [I/E]: confirm required operator deletion semantics, then authenticated recent-auth guarded HTTP/UI or explicitly justified retirement; never execute real backup deletion. |
| 04 | `GET\|HEAD admin-panel/backups/{backup}/download` | `BackupService::readLocal(Admin, backupRecordId)` already enforces `backups.manage` and exact private path, but **no protected Admin HTTP download route/UI found** in current `backend/routes/identity.php`/`integrations.tsx`. | OPEN [I/E]: decide/document acceptable intentional retirement or implement an authorized scoped read endpoint/UI and negative owner/path tests; service code is NOT UI acceptance. |
| 05 | `GET\|HEAD admin-panel/control-center/backup-operations` | Protected `GET /internal/admin/settings/integrations` renders current Google Drive backup schedule/retention and last backup status; old multi-realm control page retired. | Current React `integrations.tsx` and backend safe projection; reuse existing service/backend tests. |
| 06 | `PUT admin-panel/control-center/backup-operations` | Protected `PUT /internal/admin/integrations/google_drive/backup-settings` -> `IntegrationController::backupSettings` -> `IntegrationManager` with role + recent-auth. Old source separate database and credential setup retired. | Reuse previous W08/P08 integration controls; no real external backup or source config migration. |
| 07 | `GET\|HEAD admin-panel/control-center/integration-status` | Protected `GET /internal/admin/settings/integrations` (`IntegrationController::page`) and `GET /internal/admin/integrations` (`IntegrationManager::statuses`) display masked configured provider/backup status. | Accepted shared W08/P08 readiness; separate legacy route retired. Real provider health unverified. |
| 08 | `GET\|HEAD shop/backups` | `BackupService::history(Admin)` exists and checks `backups.manage`, but **no protected Admin backup-history route/UI found** in current `routes/identity.php`/`integrations.tsx` (current status displays only last backup). | OPEN [I/E]: verify required per-backup browsing contract and decide bounded authenticated history UI/API or approved explicit retirement with evidence. |
| 09 | `POST shop/backups` | Protected `POST /internal/admin/integrations/google_drive/backup-now` -> `BackupService::request` exists for allowed connected Google Drive; original Shop/Admin/SuperAdmin duplicate forms/credentials retired. | REUSE existing P08/W08 synthetic status/disabled-service proof; activation and authentic remote remain independent external HOLD. |
| 10 | `DELETE shop/backups/{backup}` | `BackupService::delete(Admin, backupRecordId)` already checks permission/safe local path/remote status, but **no protected Admin HTTP delete route/UI found** in current routes/React. | OPEN [I/E]: confirm required operator deletion semantics, then authenticated recent-auth guarded HTTP/UI or explicitly justified retirement; never execute real backup deletion. |
| 11 | `GET\|HEAD shop/backups/{backup}/download` | `BackupService::readLocal(Admin, backupRecordId)` already enforces `backups.manage` and exact private path, but **no protected Admin HTTP download route/UI found** in current `backend/routes/identity.php`/`integrations.tsx`. | OPEN [I/E]: decide/document acceptable intentional retirement or implement an authorized scoped read endpoint/UI and negative owner/path tests; service code is NOT UI acceptance. |
| 12 | `GET\|HEAD super-admin/backups` | `BackupService::history(Admin)` exists and checks `backups.manage`, but **no protected Admin backup-history route/UI found** in current `routes/identity.php`/`integrations.tsx` (current status displays only last backup). | OPEN [I/E]: verify required per-backup browsing contract and decide bounded authenticated history UI/API or approved explicit retirement with evidence. |
| 13 | `POST super-admin/backups` | Protected `POST /internal/admin/integrations/google_drive/backup-now` -> `BackupService::request` exists for allowed connected Google Drive; original Shop/Admin/SuperAdmin duplicate forms/credentials retired. | REUSE existing P08/W08 synthetic status/disabled-service proof; activation and authentic remote remain independent external HOLD. |
| 14 | `DELETE super-admin/backups/{backup}` | `BackupService::delete(Admin, backupRecordId)` already checks permission/safe local path/remote status, but **no protected Admin HTTP delete route/UI found** in current routes/React. | OPEN [I/E]: confirm required operator deletion semantics, then authenticated recent-auth guarded HTTP/UI or explicitly justified retirement; never execute real backup deletion. |
| 15 | `GET\|HEAD super-admin/backups/{backup}/download` | `BackupService::readLocal(Admin, backupRecordId)` already enforces `backups.manage` and exact private path, but **no protected Admin HTTP download route/UI found** in current `backend/routes/identity.php`/`integrations.tsx`. | OPEN [I/E]: decide/document acceptable intentional retirement or implement an authorized scoped read endpoint/UI and negative owner/path tests; service code is NOT UI acceptance. |
| 16 | `GET\|HEAD super-admin/control-center/activity` | Already disposed in `MT-7.5_P01_ROUTE_CROSSWALK.md` row 39 -> protected `/internal/admin/pos/audit` read-only redacted viewer. | REUSE P01 exact source-route mapping, not another H01 test. |
| 17 | `GET\|HEAD super-admin/control-center/backup-operations` | Protected `GET /internal/admin/settings/integrations` renders current Google Drive backup schedule/retention and last backup status; old multi-realm control page retired. | Current React `integrations.tsx` and backend safe projection; reuse existing service/backend tests. |
| 18 | `PUT super-admin/control-center/backup-operations` | Protected `PUT /internal/admin/integrations/google_drive/backup-settings` -> `IntegrationController::backupSettings` -> `IntegrationManager` with role + recent-auth. Old source separate database and credential setup retired. | Reuse previous W08/P08 integration controls; no real external backup or source config migration. |
| 19 | `GET\|HEAD super-admin/control-center/integration-status` | Protected `GET /internal/admin/settings/integrations` (`IntegrationController::page`) and `GET /internal/admin/integrations` (`IntegrationManager::statuses`) display masked configured provider/backup status. | Accepted shared W08/P08 readiness; separate legacy route retired. Real provider health unverified. |

## Genuine pending H01 acceptance, without reopening previously PASS tests

The first evidence-backed missing *interface* is the absence of operator-accessible **per-backup history, safe download and deletion** despite implemented, previously accepted backend-only `BackupService::history`, `readLocal` and `delete`. P08 development readiness remains accepted; H01 must determine whether these legacy UI actions are still required under the approved target user-visible contract or have an explicitly approved, documented retirement. If retained, provide only permissioned target Admin routes/UI, safe path/history guards and **new focused tests** for the missing HTTP/UI boundary; do not rerun `OperationalRecoveryTest` or previously PASS existing integration tests unchanged. Source backup/admin/shop role duplication, separate database, original credentials, live cloud/deletion and production restore remain retired or held, not test prerequisites.

The H01 comprehensive source inventory remains 1,224 files / 316 routes; this crosswalk is a **specific 19-route P08 disposition with three genuine I/E open interface behaviors**, not claim of equivalence, P08 regression, or H01 DONE.
