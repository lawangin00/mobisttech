# MT-7.5 / 06-P01 / D03 â€” archival obligation matrix (20-Sep-2026)

Owner decision D03=B authorizes history-preserving, audited, read-only archival after tests and migration checks. This matrix is a technical acceptance boundary, not authority to archive any real outlet. Current scope: synthetic data only.

| Record family | Current policy | Required proof before expanding archive eligibility |
| --- | --- | --- |
| Closed cash sessions and reviewed cash entries | Allowed to remain in archive; never deleted or reassigned. | Protected read-only summary, amounts/closing hash preserved, no new session/tender/refund after archive; full application-level cross-connection write/archive interleaving remains open. |
| Open cash session, pending cash entry | Block archive. | Demonstrate settlement/approval/closure before archival, not implicit cancellation. |
| Sales, invoices, sale returns, tender/refund/settlement records | Block archive even if completed. | Preserve original outlet, customer documents, immutable amounts and linked stock, refund/warranty obligations; owner-only archived reporting and multi-path write barriers. |
| Product definitions, stock units, stock movements, stocktake, stock acquisitions | Block archive, including zero-quantity product definition. | Zero-stock definition and populated acquisition/stock-history archival blockers confirmed 409 with original records retained. Inventory acquire/adjust/IMEI/product archive/replay now share locked-outlet recheck; synthetic archived receipt/replay/adjust denied 403. Physical/on-hand/reserved/transit reconciliation, stocktake start/count/recount/approval now lock the archive-shared outlet row before authorization and idempotent replay; archived start/count/replay/approval return 403 without changing preexisting count history (synthetic focused 1/1 PASS, 11 assertions). Stock-transfer create/dispatch/receive now lock both source and destination outlets in stable ID order and reject archived participants before mutation or completed-key replay (synthetic 403 test PASS). Product unit-attribute edits now hold the archive-shared outlet lock before authorization; synthetic stale-model archived edit denied HTTP 403 without changing unit color/version, linked product, or domain events (focused 1/1 PASS, 5 assertions). Protected Full Access archived-history read-only projection now includes bounded product/movement summaries and unit/acquisition/stocktake/transfer counts, excludes supplier identity, acquisition documents, private notes and IMEIs; owner/non-owner HTTP role and unchanged-row tests PASS on a synthetic forced-archived product-bearing outlet. Full historical stock archival, timed cross-connection stocktake/transfer/claim races and complete liability/transfer reconciliation remain OPEN. Isolated inventory-service product archive vs an independently held outlet lock timed out MySQL 1205; a fresh invocation after synthetic archive COMMIT returned 403 with unchanged product/idempotency history (one service path, not actual archive of product-bearing outlet). |
| Claims, warranty intake, paid repairs, trade-ins | Block archive regardless of present status. | Synthetic claim open/update/replay archived-outlet denial 403 and preserved hashed events tested. Terminal lifecycle, protected historical detail/report, warranty liabilities, cross-connection claim races and other claim-adjacent writers remain OPEN. |
| Purchase orders, suppliers, reorder policies, cash balances, promotions | Block archive; unpaid or future activity must not be hidden. | Resolve outstanding obligations and preserve account/audit history without treating historical records as deletable. |
| Website orders, reservations, fulfillment, external callbacks | Block through direct outlet links/stock transfer or active obligation rules; external provider actions remain OFF. | Reconcile cross-outlet/cross-system references and in-flight writes before any eligible classification. |
| Stock transfers (source/destination/receipt/lines) | Explicit block for either side even when `outlet_id` absent. | Create, dispatch, receipt and completed-key replay lock both outlet rows in stable ID order and deny an archived participant 403 before custody changes; synthetic transfer, receipt hash and balances remain unchanged on blocked attempts (1/1 PASS, 16 assertions). Actual historical transfer archival, independent cross-connection race, protected archived transfer/report reads and reconciliation of in-transit obligations remain OPEN. |
| Identity/outlet assignments, identity/team/operation audit | Preserve and audit; archived outlet excluded from operator selection/access. | Full Access read-only history, no membership reassignment/code reuse or audit record removal. |
| Unknown/new outlet-linked table | Fail closed. | Explicit classification, immutable-history/read-only and concurrency acceptance first. |

**Concurrency rule:** all business mutation paths capable of creating or altering archived-outlet records must serialize through the same outlet row lock as archival and re-read active status *after acquiring the lock*, including idempotent replay handling. Tested: cash open and cash tender/refund lookup reject stale archived snapshots; stock-transfer create now rechecks BOTH locked outlets and rejects a stale archived destination. A dedicated isolated two-MySQL-connection test proves the outlet row FOR UPDATE is mutually exclusive (1205 lock timeout), then the waiting connection observes the committed archived state. This is database lock-order evidence, not a timed full application-service archive/write interleaving or proof for all other sales/inventory/claim paths. No source-data migration, production archive, live payment, or automatic archival is authorized by this matrix.

**Service-level archive/cash-open overlap (synthetic only):** The actual archive service holds its outlet row lock while an independently bound second connection calls the real cash-open service. It sees pre-commit open state, waits for the row lock and receives bounded MySQL 1205; a new call after archive COMMIT is denied 403 without cash or idempotency residue. The prior three failed exploratory fixtures incorrectly reused original-connection Eloquent models; independently binding actor and outlet models resolved LOOP_GUARD. This is a bounded lock-contention/post-commit denial acceptance, not a proof for in-flight same-request completion or any other service.

**D03 historical invoice/sale guard checkpoint (20-Sep-2026):** Existing sale+invoice+product outlet AND independently invoice-only outlet both return archive HTTP 409 with original linked amounts, IDs and stock untouched; no archive audit generated. `SalesOperations::mutate` now takes the same locked outlet row and reauthorizes *before* both new mutations and completed-key replay, stopping stale archived-outlet sale/return processing. Synthetic direct archived-state sale and replay return 403 without new invoice/sale. Focused `SalesOperationsTest` 1/1 PASS (14 assertions); neighboring pre-final SalesOperations/PosShell/PosPayment 22/22 PASS (516 assertions) on final scoped candidate. **No invoice/sale/product archive eligibility approved**: return, claims, documents, customer obligations, reporting and remaining write paths require separate retention/authorization/proof. D03/P01 OPEN; 6/27 DONE (22.22%); MT-7.6 unstarted.
