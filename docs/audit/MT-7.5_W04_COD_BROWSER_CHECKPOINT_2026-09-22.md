# MT-7.5 W04 — protected Admin COD browser acceptance checkpoint (22-Sep-2026)

**State: In Progress; protected COD browser acceptance NOT YET PASSED.** MT-7.5 remains 15/27 DONE and 12 OPEN. W04 and H-02 remain open. No genuine external merchant adapter or credentials have been activated.

## Actual failure and retained positive evidence

- The original synthetic protected Admin COD draft/publish/reload/compensating-rollback browser test was added at source `c547e5684f51358755aef5e47093dbb27994c13c`. Independent test checks that an Admin without `website.payments.manage` cannot read protected settings/channels. The POS/Admin Playwright step of later full run `35762276392` reported **36 passed, 1 skipped, 1 failed**. Its sole failing COD browser test received HTTP **303** at `POST /internal/admin/website/payment-settings/drafts`, but after five seconds **no `Latest draft v...` was rendered**; the browser test did not independently prove that the policy row had been persisted. Treat this as an unverified write/redirect/UI rehydration boundary, not a proven database or provider failure. The separate permission-denial case passed.
- In that run, backend regression passed **357 tests / 10,284 assertions** and style/build, MySQL concurrency/reset and other preceding browser gates passed. The full run nevertheless concluded **FAILURE** and downstream browser and cleanup gates were skipped; prior Website-only build pass never counted as COD browser acceptance.

## Corrective source and new independent gate

- Commit `fa29a69fdb7127c8f8ab922754d1c10ee961979a` replaces the ambiguous Inertia `router.post`/303 mutation path with the existing authenticated Admin CSRF endpoint plus an explicit same-origin JSON POST. COD draft must return HTTP 201 and a revision record; publish must return HTTP 200 and a revision record. Only after confirmed server success does the UI refresh Inertia props with `preserveState: false`, rehydrating the draft and published checkout policy. Errors are shown in the protected UI. This is a narrow nonsecret COD UI transport change, not weaker CSRF or new provider activation.
- Browser test source commit `69a82e6f056c6166ed2e2d38d62445a34918986d` asserts those actual JSON response contracts, revision IDs/versions, correct COD boolean and monotonically newer compensating rollback version, then checks draft isolation, published channel availability, reload persistence and still-unavailable external providers. Disposable CI-only COD revision cleanup remains guarded to the synthetic owner and isolated MySQL database.
- Exact-source full [CI run 35767738976](https://github.com/lawangin00/mobisttech/actions/runs/35767738976) and independent [Website build run 35767738993](https://github.com/lawangin00/mobisttech/actions/runs/35767738993) were triggered by request-only commit `27bcab132fda14d258525b4758f811f22d36b762` pinned to source `69a82e6f056c6166ed2e2d38d62445a34918986d`. Full CI exact-request validator passed; final acceptance outcome was still pending at this checkpoint. Neither run may be called PASSED before its terminal success.

## First pending bounded action

Inspect the terminal result of full run `35767738976`, especially the real protected COD Playwright case and cleanup; inspect and fix an actually observed failure rather than assume a provider error. When full CI passes, record its exact source and terminal evidence without closing W04: real merchant integration/approval and authentic provider callback/payment/refund remain HOLD. Keep historical failures distinguishable from any future successful run, and never mutate production or legacy source repositories as part of synthetic COD acceptance.
