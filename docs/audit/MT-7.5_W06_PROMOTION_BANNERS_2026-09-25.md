# MT-7.5 W06 versioned Website announcements and banners (25-Sep-2026; LOCAL)

Parent SHA: `d716dcd58bec5950451e5964fa0febdf6e16048c`. Bounded W06 functional implementation and synthetic/API verification, not family closure.

- Reuses existing protected permission-separated `website.presentation` revisions. Strictly typed `promotion` snapshot supports one announcement and up to six plain-text/banner records with optional HTTPS/allowlisted safe local links and explicit Website mode scopes. Unknown fields, unsafe URL schemes, protected app destinations and oversized/banner-count invalid entries deny with HTTP 422. This is independent display-only content; coupons/discount calculations remain their separate existing server authority.
- A saved draft changes nothing publicly. On authenticated publish/rollback, existing `cms.presentation.promotion` pointer is applied and `cms.presentation` versioned cache invalidated. The public content index returns only mode-allowed announcement/banner records; Next site header announcement and homepage banner cards render plain text + safe links, never arbitrary HTML. No production advertising claims supplied.
- Isolated local PHP promotion + adjacent CMS/navigation **3/3 PASS (45 assertions)**; existing affected real Website `dynamic-content.spec.ts` **4/4 PASS (2.2m)** after Home/SiteHeader/index changes, with guarded fixture teardown and production Website build; PHP syntax/Pint, Next typecheck and diff check PASS. Real protected Admin -> published public banner browser journey remains OPEN. Do not treat existing homepage neighboring tests as direct announcement/banner E2E acceptance.
- W06 remains OPEN for joined navigation/banner authoring/browser, global SEO, managed-page media and other finite family checks; 27-gate progress unchanged.
