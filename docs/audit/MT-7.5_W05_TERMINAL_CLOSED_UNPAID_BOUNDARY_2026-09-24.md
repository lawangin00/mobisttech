# MT-7.5 / W05 real terminal-state boundary (24-Sep-2026, LOCAL)

W05 remains OPEN [E]; MT-7.5 development checklist remains 16/27 DONE, 11 OPEN. Parent source `76f1dd6dfc5321f161c859b3366928d2a4560eab`; test-only change in existing real Admin -> Customer joined browser journey. W04 DEVELOPMENT DONE, H-02 authentic vendor payment PRE-LAUNCH PENDING, external methods OFF. No backend/Website product code, vendor, production or protected source change.

- After genuine approved -> in_progress -> review -> delivered HTTP 200 transitions for one newly created owned project with an unpaid approved PKR 10000.00 milestone, the actual authenticated Admin React UI attempts completed. Real protected PATCH returns **409** (unpaid milestone cannot be completed); then real Admin transitions delivered -> **closed** with PATCH **200** and returned `closed`. The owner Customer's real portal shows `closed`, keeps original proposal amount/scope and four immutable project-status events, and hides the reference-upload form while remaining accessible in `commerce_only`. A closed project is **not** a paid/completed project and no genuine provider receipt is fabricated.
- Isolated Microsoft Edge focused W05 test **1/1 PASS** (50.6s), followed sequentially by affected real Admin/private-delivery + status browser join **2/2 PASS** (1.2m), both terminal exit 0 under `tools/run_isolated_test.py` cooperative lock for isolated `mobisttech_test@127.0.0.1:13306`. Guarded fixture setup and teardown completed; no overlapping runner observed at preflight, no database-wide ownership/residue PASS claimed from teardown alone. The first attempted wrapper invocation failed **ORCHESTRATION** before tests: Python on Windows could not spawn the `npx` shell shim; corrected to the explicit `npx.cmd` executable after a successful exclusive-lock smoke test. No unchanged failed suite was retried.
- Unchanged PHP `ClientProjectServicesTest::test_completion_requires_all_milestones_and_mode_switch_preserves_portal_files` already proves fake-receipt-paid **completed** service transition and retained private delivery under `commerce_only`; the prior current W04 family 117/117 PHP includes this W05 case. Neither synthetic receipt nor closed-state HTTP/UI proves authentic vendor settlement. No broad PHP/backend/hosted regression repeated for browser-only change.

Next executable W05 step: reconcile remaining **expiry and signed-in foreign Customer/mode** boundary evidence against current backend and actual Website/API tests; add only a demonstrably missing bounded negative, then execute the applicable joined W05 family closure. Preserve 24/9 source mapping, accepted W04 development, H-02 pre-launch HOLD and independent Q01 exact-release acceptance.