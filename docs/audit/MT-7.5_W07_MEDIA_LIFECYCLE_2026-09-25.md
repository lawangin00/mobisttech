# MT-7.5 W07 private media lifecycle and protected Admin acceptance (25-Sep-2026 LOCAL)

**Scope:** independent W07 Website CMS media library and safe replacement/deletion, not W07 complete. Parent theme checkpoint `420eb84aedc6917ca595395ea890cb81da127537`; original protected source read only.

**Behavior:** Existing MIME/extension/signature/image-size validation and private `local` CMS UUID asset storage preserved; reject SVG, untrusted paths, foreign roles and unsafe existing legacy public-disk assets. Alt text update is validated and audited. Replacement uploads a NEW private asset ID with independently verified bytes and immutable historical/current references; it never silently rewrites media IDs in published snapshots or does a mutable in-place byte swap. Separate re-assignment plus version publication remains mandatory. The guarded DELETE action requires recent authenticated Website media permission, disallows known `site_media_usages`, current/draft/historical page/Software/presentation snapshot references and current presentation settings; only an unreferenced private asset is tombstoned first and its exact private file then removed. Retired or foreign assets cannot be revived by the SHA dedup path. Admin Content media library shows each asset, safe alt editor, explicit new-ID replacement and prompted unused-only deletion; no public media index or anonymous file access added.

**Proof:** Exclusive local `mobisttech_test@127.0.0.1:13306` PHP `WebsiteCmsTest.php` + new `WebsiteMediaLifecycleTest.php` **11/11 PASS (185 assertions)** `.local/w07_media_php_final.log`, covering private bytes, spoof/SVG denial, per-asset owner/role guard, draft/historical reference 409, explicit usage, branding revision and old/new ID integrity. Protected Microsoft Edge `backend/tests/browser-website/theme-w07.spec.ts -g 'W07 media Admin'` **1/1 PASS (1.5m)** `.local/w07_media_edge1.log`: actual private Admin uploads one synthetic image, saves alt, creates separate replacement, confirms original remains active until unused-only delete, second stays active until independent delete; public homepage unaffected. Verified exact synthetic owner/path guarded `W07MediaE2eCleanupSeeder` and global teardown PASS. Backend typecheck and Vite build exit 0, syntax and scoped Pint after formatting correction PASS; only initial formatting issues were fixed, no failed media browser attempt. Prior W07 theme 1/1 Edge and W06 7/7 unchanged evidence reused, not re-run.

**Limits:** In-use replacement is explicit safe new asset + manual re-assignment, not original in-place byte mutation; brand role image publication and Website secret/recovery have separate pending acceptance. This media checkpoint is accepted, but `20/W07` remains OPEN [I,E] and MT-7.5 is still 18/27 DONE, 9 OPEN.