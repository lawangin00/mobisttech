# MT-7.5 W04 nonsecret channel presentation first implementation checkpoint (24-Sep-2026 PKT)

Owner-approved scope: channel labels and instructions, COD minimum/maximum, authorized Admin editing, and new-orders-only effects. This checkpoint implements only strictly validated, published nonsecret channel label/instruction consumption in checkout API and Website checkout UI; the draft/publish Admin writer, COD bounds enforcement, historical-order immutable-term contract and owner/delegate lifecycle are still OPEN. The new independent `website.payments.presentation` domain cannot modify the existing `website.payments.cod` on/off revisions and cannot register or enable a payment adapter. Malformed presentation revisions fall back to defaults. Existing orders are not edited by this checkpoint. No provider credentials, live integration, or real transaction.

Focused synthetic tests: `W04PaymentPresentationValidationTest` 2/2 PASS (17 assertions) including MySQL JSON reordering and external gateways remaining unavailable after published label changes. Neighboring W04 Admin/COD/commerce suites 47/47 PASS (442 assertions); Website `npm run typecheck` PASS; scoped Pint PASS and diff whitespace PASS. First focused attempt failed because JSON columns reorder keys; validator corrected to reject unknown/missing fields independent of JSON object key order, then focused tests PASS. New fields' public checkout acceptance is API/backend and TypeScript only, not full browser or hosted CI acceptance.

Next: protected, version-controlled nonsecret Admin draft/publish editor with authorized Admin grants; then atomic COD amount bounds at NEW checkout only with immutable order-term snapshots and explicit negative/replay evidence. Preserve existing COD and external payment behavior until those independent tests pass. W04/MT-7.5 IN PROGRESS 15/27 DONE / 12 OPEN; H-02 genuine provider contract/credential/callback/refund/settlement HOLD.